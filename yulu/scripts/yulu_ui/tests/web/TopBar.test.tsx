// Moved to ./components/TopBar.test.tsx — see new file for the canonical
// multi-segment breadcrumb tests. This file is retained as an empty stub so
// nothing imports a stale path; remove once tooling tolerates renames cleanly.
import { describe, it } from "vitest";

describe("TopBar (moved)", () => {
  it("see tests/web/components/TopBar.test.tsx", () => {});
});
