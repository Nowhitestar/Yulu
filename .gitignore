# Secrets (config is per-user, never commit it)
config.json
.env
.env.*
client_secret*.json
credentials*.json
token*.json
*.pem
*.key
*.p8
*.p12

# Compiled objects
__pycache__/
*.py[cod]
*.egg-info/
dist/
build/

# macOS
.DS_Store
*.swp
*.swo
*~

# Compiled binaries (regenerated by build_audio_daemon.sh / setup.sh)
yulu/scripts/recorder_status
yulu/scripts/window_scanner
yulu/scripts/audio_daemon

# IDE
.idea/
.vscode/
*.swp

# Local recordings, transcripts, and agent state (per-user, never share)
meeting-recordings/
*.transcript.txt
*.summary.md
*.whisper.txt
agent-queue.json
schedule.json

# Logo raster outputs (regenerate from SVG)
assets/logo.png
assets/logo-flat.svg
assets/social-card.png
