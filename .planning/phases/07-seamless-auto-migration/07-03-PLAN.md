---
phase: 07-seamless-auto-migration
plan: 03
type: execute
wave: 2
depends_on: ["07-01", "07-02"]
files_modified:
  - yulu/scripts/migrate/apply.py
  - yulu/scripts/migrate/verify.py
  - yulu/scripts/migrate/cli.py
  - yulu/scripts/yulu
  - tests/test_migrate_apply_rollback.py
  - tests/test_migrate_corrections.py
autonomous: false
requirements: [MIG-01, MIG-02, MIG-03]
tags: [migration, transactional, rollback, backup, schema_version, in-transit-corrections]

must_haves:
  truths:
    - "Migration is transactional: a backup is taken BEFORE apply; a failed verify keeps the backup and `yulu rollback` restores the prior state byte-for-byte"
    - "The backup is pruned ONLY after verify passes (a failed verify never prunes — never a silent half-migration)"
    - "apply drops the dead transcription.mlx.python field, routes ~/Movies/Yulu via PathResolver, stamps schema_version, and PRESERVES .yulu-install.json source"
    - "No data loss: recordings/transcripts/vocab/prompts/summaries survive the migration"
    - "apply calls the recording-guard before stopping daemons (refuses while recording); no pkill -9"
    - "`yulu migrate` (detect->plan->apply->verify, --dry-run-able) and `yulu rollback` are wired into the CLI dispatcher"
  artifacts:
    - path: "yulu/scripts/migrate/apply.py"
      provides: "transactional backup->guarded-stop->apply-corrections->mark; rollback restore"
      exports: ["apply_migration", "rollback", "MigrationResult"]
      min_lines: 70
    - path: "yulu/scripts/migrate/verify.py"
      provides: "post-migration doctor host_capabilities + schema_version check + prune-on-success-only"
      exports: ["verify_migration", "prune_backup"]
      min_lines: 50
    - path: "yulu/scripts/migrate/cli.py"
      provides: "yulu migrate (detect->plan->apply->verify, --dry-run) + yulu rollback"
      exports: ["main"]
      min_lines: 60
    - path: "tests/test_migrate_apply_rollback.py"
      provides: "transactional backup/rollback/prune-on-verify-only/no-data-loss coverage"
      contains: "def test_"
    - path: "tests/test_migrate_corrections.py"
      provides: "drop mlx_python / route Movies-Yulu / stamp schema_version / preserve source coverage"
      contains: "def test_"
  key_links:
    - from: "yulu/scripts/migrate/apply.py"
      to: "migrate.guard.stop_daemons_guarded"
      via: "recording-guarded daemon stop before mutating the tree"
      pattern: "stop_daemons_guarded|RecordingActive"
    - from: "yulu/scripts/migrate/apply.py"
      to: "release_installer.move_existing_runtime_to_backup / restore_backup"
      via: "transactional backup + rollback restore"
      pattern: "move_existing_runtime_to_backup|restore_backup"
    - from: "yulu/scripts/migrate/apply.py"
      to: "provision.state.mark"
      via: "schema_version stamp preserving installer source (Pitfall 3)"
      pattern: "state.mark|schema_version"
    - from: "yulu/scripts/migrate/verify.py"
      to: "doctor host_capabilities"
      via: "post-migration capability re-detection gates the prune"
      pattern: "doctor|host_capabilities"
    - from: "yulu/scripts/yulu"
      to: "migrate.cli"
      via: "migrate) / rollback) dispatcher cases"
      pattern: "migrate.cli"
---

<objective>
Compose the destructive half of the pipeline (D-01, D-03, D-04, D-05) on top of Plan 01's detect/plan and Plan 02's recording-guard: `migrate/apply.py` runs transactionally — backup the prior state BEFORE apply (reusing `release_installer` backup/restore primitives), stop daemons through the recording-guard (refusing while recording, no `pkill -9`), apply the in-transit corrections (drop dead `transcription.mlx.python`, route hardcoded `~/Movies/Yulu` via `PathResolver`, stamp `schema_version` via `provision.state.mark` PRESERVING `source` — Pitfall 3), and mark the ledger; `migrate/verify.py` re-detects capability via the Phase 3 `doctor` host_capabilities report and prunes the backup ONLY on a pass (a failed verify keeps the backup and offers `rollback`); `yulu rollback` restores the prior state byte-for-byte. `migrate/cli.py` wires `yulu migrate` (detect->plan->apply->verify, `--dry-run`-able) and `yulu rollback` into the dispatcher. A human-verify checkpoint covers the two live-install confirmations that cannot be faked.

Purpose: This delivers MIG-01 (the full pipeline, no data loss, no reconfiguration), MIG-03 (transactional + rollback + prune-only-on-success), and the apply-side of MIG-02 (guarded stop). NO data is lost and NO half-migration is ever left silently.

Output: `migrate/apply.py`, `migrate/verify.py`, `migrate/cli.py`, a `yulu` dispatcher edit, two Wave-0 test files, and a `SUMMARY.md`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-seamless-auto-migration/07-CONTEXT.md
@.planning/phases/07-seamless-auto-migration/07-VALIDATION.md

@yulu/scripts/release_installer.py
@yulu/scripts/provision/state.py
@yulu/scripts/provision/cli.py
@yulu/scripts/yulu
@yulu/scripts/config.example.json
@yulu/scripts/migrate/detect.py
@yulu/scripts/migrate/plan.py
@yulu/scripts/migrate/guard.py
</context>

<interfaces>
Contracts this plan binds to (already in the codebase — do NOT re-implement):

- `migrate.detect.detect_migration(runtime_dir, config_dir) -> MigrationNeed` and `migrate.plan.build_plan(need) -> MigrationPlan` (Plan 01). The CLI runs detect -> build_plan -> (dry-run prints `plan.render()` and STOPS) -> apply -> verify.
- `migrate.guard.stop_daemons_guarded(labels, manager, socket_send)` raising `migrate.guard.RecordingActive` (Plan 02). apply MUST call this before touching the tree; on `RecordingActive` it aborts cleanly (no backup consumed, no mutation, exit non-zero with the "recording active — refused" message).
- `release_installer.move_existing_runtime_to_backup(install_dir) -> Path | None` moves the existing `install_dir` aside to a `tempfile.mkdtemp(prefix="<name>.backup-")` sibling and returns the backup path (the primitive to REUSE for the pre-apply backup). `restore_backup(backup, install_dir)` moves the backup back (byte-for-byte via `shutil.move`). `replace_runtime_with_backup` / `install_release_from_urls` (line 476) show the backup -> try -> except-restore transactional shape to mirror.
- `provision.state.mark(path, step, status, detail="") -> dict` writes `.yulu-install.json` atomically (mkstemp+os.replace) and ADDS `schema_version` (`state.SCHEMA_VERSION`) + a `steps` entry while PRESERVING `_INSTALLER_KEYS` (`source`/`version`/`sha256`/`installed_at`...) via setdefault — this is the schema_version stamp AND the Pitfall-3 source-preserve in one call. Use it; do NOT hand-write the ledger. `state._atomic_write(path, doc)` is the atomic temp+replace idiom to mirror for the config.json rewrite.
- The dead config field is `transcription.mlx.python` (config.example.json line 34-37). The recording-path default to route is `~/Movies/Yulu` (PathResolver `_DEFAULT_DATA_SUBDIR`); apply rewrites a hardcoded `~/Movies/Yulu` in `config.json` `audio.output_dir` to the resolver-derived path, or leaves an already-configured custom output_dir UNTOUCHED (correct-in-transit, never reconfigure the user's chosen folder). Config lives at `runtime_dir()/config.json` (`~/.config/yulu/config.json`), SEPARATE from the runtime tree at `~/.yulu`.
- `doctor.py main(["--json"])` prints the report including `host_capabilities` `{schema_version, capabilities, [error]}`; `doctor._host_capabilities(config_dir, runtime_root)` returns it directly. verify runs the doctor report and treats a structurally-valid report (no `error` key, capabilities present) as the post-migration health gate (MIG-01 "verify"). Invoke doctor with a FIXED argv + JSON parse, or call `_host_capabilities` directly (resolve-not-execute; mirror the `capabilities` tRPC router / provision attest idiom).
- The eight `com.yulu.*` labels and `MacOSDaemonManager` clean-stop come from the guard (Plan 02) — apply does not re-enumerate them, it passes `labels=None` to let the guard default.
- CLI idiom: mirror `provision/cli.py` (argparse subcommands, module `main`). The `yulu` dispatcher cases `provision)`/`skill)` (yulu lines 311-312) use `PYTHONPATH="$SCRIPT_DIR:..." exec "${PYTHON:-python3}" -m <module> ...`. Add `migrate)` and `rollback)` the same way (`-m migrate.cli migrate` / `-m migrate.cli rollback`).

Contract this plan DEFINES:

- `MigrationResult` — frozen dataclass: `ok: bool`, `backup: Path | None`, `reasons: list[str]`, `rolled_back: bool`.
- `apply_migration(need, plan, runtime_dir, config_dir, *, manager=None, socket_send=None) -> MigrationResult` — the transactional driver: guard-stop -> backup -> apply corrections -> state.mark -> return result carrying the backup path.
- `rollback(backup, install_dir) -> None` — restore prior state from the backup (wraps `restore_backup`).
- `verify_migration(config_dir, runtime_root) -> bool` — post-migration doctor gate.
- `prune_backup(backup) -> None` — remove the backup dir (called ONLY after verify passes).
- `finalize(result, config_dir, runtime_root) -> bool` — verify gate + prune-on-success-only (in verify.py).
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: migrate/apply.py — transactional backup -> guarded-stop -> corrections -> mark; rollback restore (MIG-01, MIG-02, MIG-03, D-03, D-04)</name>
  <files>yulu/scripts/migrate/apply.py, tests/test_migrate_apply_rollback.py, tests/test_migrate_corrections.py</files>
  <read_first>
    - yulu/scripts/release_installer.py (move_existing_runtime_to_backup line 408, restore_backup line 383, replace_runtime_with_backup line 359 — REUSE not rewrite; install_release_from_urls line 476 = the backup->try->except-restore transactional shape to mirror)
    - yulu/scripts/provision/state.py (mark line 178 = schema_version stamp + Pitfall-3 source preserve; _INSTALLER_KEYS line 81; _atomic_write line 138 = the config-rewrite idiom)
    - yulu/scripts/migrate/guard.py (Plan 02 — stop_daemons_guarded, RecordingActive)
    - yulu/scripts/migrate/plan.py (Plan 01 — MigrationPlan / PlanStep.kind values the apply dispatches on)
    - yulu/scripts/config.example.json (transcription.mlx.python line 34, audio.output_dir line 20)
    - .planning/phases/07-seamless-auto-migration/07-CONTEXT.md (D-03 transactional, D-04 corrections)
  </read_first>
  <behavior>
    - Test (transactional order): `apply_migration` takes a backup of the prior state BEFORE applying any correction (the returned backup dir exists and equals the pre-apply tree/config snapshot).
    - Test (recording-guard, MIG-02): with an injected status reporting recording=true, `apply_migration` returns a refused result (ok=False, reasons include "recording active"), takes NO backup-consuming action, mutates NOTHING (config.json + tree byte-identical), stops no daemon — composes Plan 02.
    - Test (drop mlx_python, D-04): after apply on a fixture config.json carrying `transcription.mlx.python`, the field is GONE and the rest of the config is preserved unchanged.
    - Test (route ~/Movies/Yulu, D-04): a config.json whose `audio.output_dir` is the hardcoded `~/Movies/Yulu` is rewritten to the resolver-derived path; a config with an already-custom output_dir is left UNTOUCHED (no reconfiguration).
    - Test (schema_version stamp + PRESERVE source, D-04 / Pitfall 3): after apply, `.yulu-install.json` has `schema_version == state.SCHEMA_VERSION` AND its original `source` ("release"/"dev") is INTACT.
    - Test (no data loss, MIG-01): a fixture tree with `recordings/`, `transcripts/`, `vocab.sqlite`, `prompts.sqlite`, summaries -> all present byte-for-byte after apply.
    - Test (rollback byte-for-byte, MIG-03): `rollback(backup, install_dir)` restores the prior tree so it is byte-identical to the pre-apply snapshot.
  </behavior>
  <action>
    Create `migrate/apply.py`. Define the frozen `MigrationResult`. Implement `apply_migration(need, plan, runtime_dir, config_dir, *, manager=None, socket_send=None) -> MigrationResult`: (1) call `migrate.guard.stop_daemons_guarded(None, manager, socket_send)` — on `RecordingActive`, return `MigrationResult(ok=False, backup=None, reasons=["recording active — refused"], rolled_back=False)` WITHOUT mutating anything; (2) take the backup BEFORE any mutation — reuse `release_installer.move_existing_runtime_to_backup(install_dir)` for the tree and snapshot `config.json` aside so rollback restores byte-for-byte; (3) walk `plan.steps`, dispatching on `kind`: `config_correction` -> load `config_dir/config.json`, `pop("python", None)` from the `transcription.mlx` dict (guard missing keys), write back via an atomic temp+replace (mirror `state._atomic_write`); `path_route` -> if `audio.output_dir` equals the hardcoded `~/Movies/Yulu` default, rewrite it to the `MacOSPathResolver().data_dir()` path, else leave the user's configured value untouched; `schema_stamp` -> call `provision.state.mark(<ledger_path>, "migrate", "ok")` (stamps `schema_version` AND preserves `source`/installer keys — Pitfall 3); (4) return `MigrationResult(ok=True, backup=<backup>, ...)`. Implement `rollback(backup, install_dir)` wrapping `release_installer.restore_backup`. On ANY exception mid-apply, restore from the backup and re-raise (mirror `install_release_from_urls`'s backup->except->restore) — never leave a half-migration. stdlib only; lazy/guarded imports for record_audio/PathResolver so the module imports off-Darwin.
  </action>
  <verify>
    <automated>make pytest 2>&1 | tail -20 ; python3 -m pytest tests/test_migrate_apply_rollback.py tests/test_migrate_corrections.py -q 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - Backup is taken BEFORE any mutation; rollback restores the prior tree byte-for-byte.
    - Recording active -> refused result, ZERO mutation, no daemon stopped (composes Plan 02's guard).
    - mlx_python dropped, ~/Movies/Yulu routed (custom output_dir left alone), schema_version stamped == state.SCHEMA_VERSION, `.yulu-install.json` source PRESERVED.
    - No data loss: recordings/transcripts/vocab/prompts/summaries survive byte-for-byte.
    - `migrate/apply.py` is stdlib-only and contains no `pkill`.
  </acceptance_criteria>
  <done>apply_migration is transactional (backup-first), guarded (refuses while recording), applies all three corrections preserving source, loses no data; rollback restores byte-for-byte; tests green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: migrate/verify.py — post-migration doctor gate + prune-backup-ONLY-on-success (MIG-01 verify, MIG-03)</name>
  <files>yulu/scripts/migrate/verify.py, tests/test_migrate_apply_rollback.py</files>
  <read_first>
    - yulu/scripts/doctor.py (_host_capabilities line 214, main/--json line 488, _overall_ok line 402 — the post-migration health report; schema_version line 230/280)
    - yulu/scripts/migrate/apply.py (Task 1 — MigrationResult.backup is what verify prunes)
    - yulu/scripts/provision/cli.py (the resolve-not-execute / fixed-argv idiom for invoking a Python tool)
    - .planning/codebase/CONCERNS.md (§2e backups never cleaned — bounded lifecycle is THIS plan; the prune is the fix)
    - .planning/phases/07-seamless-auto-migration/07-CONTEXT.md (D-03 "pruned ONLY after verification passes; failed verify keeps the backup + offers rollback")
  </read_first>
  <behavior>
    - Test (verify pass): with a doctor host_capabilities report that has no `error` and a non-empty `capabilities`, `verify_migration(config_dir, runtime_root) is True`.
    - Test (verify fail): with a doctor report carrying an `error` key (or empty capabilities), `verify_migration is False`.
    - Test (prune ONLY on pass, MIG-03 — the headline §2e fix): `finalize(result, ...)` PRUNES the backup when verify passes (backup dir gone) and KEEPS the backup when verify fails (backup dir still present) — assert both branches. A failed verify must NEVER prune.
    - Test (prune_backup is a no-op on None): `prune_backup(None)` does nothing and does not raise.
    - Test (schema_version surfaced): the verify report reflects the stamped `schema_version` after a successful migration (it reads the post-apply state, not the pre-apply one).
  </behavior>
  <action>
    Create `migrate/verify.py`. Implement `verify_migration(config_dir, runtime_root) -> bool`: obtain the post-migration host_capabilities report (call `doctor._host_capabilities(config_dir, runtime_root)` directly, or invoke `doctor.py --json` with a fixed argv + `json.loads` — resolve-not-execute, never interpolate a path into a shell); return True iff the report has NO `error` key AND a non-empty `capabilities` map (structurally healthy). Implement `prune_backup(backup: Path | None) -> None`: when `backup` is None, return; otherwise remove the backup directory (`shutil.rmtree`, guarded). Implement the driver helper `finalize(result: MigrationResult, config_dir, runtime_root) -> bool` (the CLI calls it): if `result.ok` and `verify_migration(...)` -> `prune_backup(result.backup)` and return True; otherwise LEAVE the backup in place and return False (the caller then offers `yulu rollback`). The prune is reached on the success path ONLY — a failed verify keeps the backup so rollback is always possible (D-03; fixes CONCERNS §2e bounded-lifecycle). stdlib only.
  </action>
  <verify>
    <automated>python3 -m pytest tests/test_migrate_apply_rollback.py -q 2>&1 | tail -15</automated>
  </verify>
  <acceptance_criteria>
    - verify passes on a structurally-healthy doctor report, fails on an error/empty report.
    - The backup is pruned ONLY when verify passes; a failed verify KEEPS the backup (both branches asserted via `finalize`) — never a silent half-migration with a discarded backup.
    - `prune_backup(None)` is a safe no-op.
    - doctor is invoked resolve-not-execute (fixed argv / direct call), never shell-interpolated.
  </acceptance_criteria>
  <done>verify_migration gates on the post-migration doctor report; finalize reclaims the backup only on a verified success and retains it on failure for rollback; tests green.</done>
</task>

<task type="auto">
  <name>Task 3: migrate/cli.py + yulu dispatcher — `yulu migrate` (detect->plan->apply->verify, --dry-run) + `yulu rollback` (MIG-01, MIG-03, D-05)</name>
  <files>yulu/scripts/migrate/cli.py, yulu/scripts/yulu</files>
  <read_first>
    - yulu/scripts/provision/cli.py (the argparse-subcommand + module-main shape to mirror; _run_all driver structure)
    - yulu/scripts/yulu (dispatcher cases provision) line 311 / skill) line 312 = the `PYTHONPATH=... exec -m <module>` idiom; usage() block lines 54-98 to extend)
    - yulu/scripts/migrate/detect.py, plan.py, apply.py, verify.py (the four pipeline stages this composes)
  </read_first>
  <action>
    Create `migrate/cli.py` mirroring `provision/cli.py` (argparse subcommands, `main(argv)`), composing the four stages — it RE-IMPLEMENTS none of them: `migrate [--dry-run] [--runtime-dir DIR] [--config-dir DIR]` runs `detect_migration` -> if not `needs_migration`, print "up-to-date" and exit 0 -> `build_plan(need)` -> if `--dry-run`, print `plan.render()` and exit 0 (no mutation) -> else `apply_migration(...)` -> on a refused (recording-active) result, print the refusal and exit non-zero -> `verify.finalize(result, ...)`: on pass print success (backup pruned); on FAIL print "verification failed — backup retained; run `yulu rollback` to restore" and exit non-zero (never prune). `rollback [--runtime-dir DIR]` locates the most-recent `<name>.backup-*` sibling and calls `apply.rollback(backup, install_dir)`, printing the restored state. Wire the `yulu` dispatcher: add `migrate)` -> `-m migrate.cli migrate "$@"` and `rollback)` -> `-m migrate.cli rollback "$@"` (the `PYTHONPATH="$SCRIPT_DIR:..." exec "${PYTHON:-python3}"` idiom, identical to provision) and extend `usage()` with the two new commands. Default mode is the safe path: a bare `yulu migrate` on an up-to-date install is a no-op; `--dry-run` never mutates. stdlib + the four migrate modules only.
  </action>
  <verify>
    <automated>make pytest 2>&1 | tail -15 ; bash -n yulu/scripts/yulu && echo "yulu syntax OK" ; PYTHONPATH=yulu/scripts python3 -m migrate.cli migrate --dry-run --runtime-dir /tmp/nonexistent-yulu --config-dir /tmp/nonexistent-cfg 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `yulu migrate` runs detect->plan->apply->verify; an up-to-date install is a no-op (exit 0, no mutation).
    - `yulu migrate --dry-run` prints the plan and mutates nothing (no apply, no daemon stop).
    - A failed verify prints "backup retained; run `yulu rollback`" and does NOT prune (exit non-zero); a recording-active refusal exits non-zero without mutating.
    - `yulu rollback` restores from the most-recent backup; `bash -n yulu/scripts/yulu` passes; the `migrate)`/`rollback)` cases dispatch to `migrate.cli`.
  </acceptance_criteria>
  <done>`yulu migrate` and `yulu rollback` are wired and compose the four stages; dry-run is mutation-free; failed verify retains the backup and points to rollback; CLI syntax valid.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Checkpoint: real v0.5.x upgrade + live-recording guard (live-install confirmations, NOT faked)</name>
  <what-built>
    The full `yulu migrate` (detect->plan->apply->verify) + `yulu rollback` pipeline: transactional backup-first apply, the recording-guard, the in-transit corrections (drop mlx_python / route ~/Movies/Yulu / stamp schema_version / preserve source), and prune-backup-only-on-verified-success. detect/plan/apply/rollback/guard/corrections are ALL fixture-tested green (Plans 01/02 + this plan's Tasks 1-3). These two confirmations require a REAL legacy v0.5.x `~/.yulu` install + the live daemon stack — they are NOT faked (07-VALIDATION.md "Manual-Only Verifications").
  </what-built>
  <how-to-verify>
    1. Real end-to-end upgrade (MIG-01). On a machine (or VM) carrying a genuine v0.5.x `~/.yulu` install (schema_version absent / old layout, with real recordings + transcripts + `vocab.sqlite` + `prompts.sqlite` + summaries):
       - Run `yulu migrate --dry-run` -> confirm it lists the planned corrections without changing anything.
       - Run `yulu migrate` -> confirm: recordings/transcripts/vocab/prompts/summaries are ALL intact afterward; daemons start; `~/.config/yulu/config.json` no longer has `transcription.mlx.python`; `audio.output_dir` is correct (a custom path was left untouched); `.yulu-install.json` carries `schema_version` AND its original `source`; and NO reconfiguration was prompted.
       - Inspect that a `<name>.backup-*` sibling was pruned after the run reported a verified success.
    2. Live-recording guard (MIG-02). Start a recording (`yulu record start "guard test"`), then while it is still recording trigger `yulu migrate` -> confirm it REFUSES to stop the audio daemon (prints the recording-active refusal, names the live recording), leaves the WAV un-truncated, and does NOT half-migrate. Stop the recording, re-run `yulu migrate` -> confirm it now proceeds.
  </how-to-verify>
  <resume-signal>Type "approved" once both the real upgrade (no data loss, no reconfig, source preserved, backup pruned on success) and the live-recording refusal are confirmed; otherwise describe what failed.</resume-signal>
  <files>(none — this is a human-verify gate; no files are produced. The pipeline files land in Tasks 1-3.)</files>
  <action>Manual, human-driven verification on a real v0.5.x install + a live recording — see <how-to-verify>. This checkpoint runs AFTER Tasks 1-3 are green; it does not write code. The agent automates everything up to here (the full fixture-tested pipeline) and pauses for the two live-install confirmations that cannot be faked (07-VALIDATION.md "Manual-Only Verifications").</action>
  <verify>
    <human-check>Both confirmed: (1) a real v0.5.x ~/.yulu upgrades with no data loss, no reconfiguration, source preserved, and the backup pruned only after the verified success; (2) `yulu migrate` refuses to stop the audio daemon while a recording is active (no truncated WAV) and proceeds once the recording ends.</human-check>
  </verify>
  <done>The reviewer typed "approved" after both live-install behaviors were observed; any failure is described instead and the plan is revised before proceeding.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| prior install state -> transactional apply | The existing tree + config + ledger are the irreplaceable user data. A crash, a bad correction, or a failed verify must NEVER leave them lost or half-migrated — the backup is the recovery boundary. |
| in-flight recording -> apply's daemon-stop | Composed from Plan 02: apply must not truncate a live capture (recording-guard refuses). |
| supply-chain (none new) | This plan installs NO packages; it only moves/edits the user's own tree. No new package-manager surface. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-08 | Tampering (data-integrity, HIGH) | apply crashing mid-mutation -> half-migrated tree | mitigate | Backup is taken BEFORE any mutation (`move_existing_runtime_to_backup` + config snapshot); ANY exception triggers restore-from-backup then re-raise (mirrors `install_release_from_urls`). A crash leaves either the old state (restored) or a verified new state — never a half-migration. |
| T-07-09 | Tampering (data-integrity, HIGH) | backup pruned before verify -> no recovery from a bad migration | mitigate | `prune_backup` is reached ONLY on the verify-pass branch (Task 2 `finalize`); a failed verify KEEPS the backup and the CLI points to `yulu rollback`. Both branches are asserted (the §2e bounded-lifecycle fix done safely). |
| T-07-10 | Tampering (HIGH) | dropping `.yulu-install.json` `source` -> next update wrongly takes the swiftc dev branch | mitigate | The schema_version stamp goes through `provision.state.mark`, which PRESERVES `source`/installer keys via setdefault (Pitfall 3 / T-06-07). A dedicated test asserts source survives the migration. |
| T-07-11 | Tampering (data-integrity, HIGH) | truncating a live recording during apply's daemon-stop | mitigate | apply calls `migrate.guard.stop_daemons_guarded` FIRST; a recording-active state returns a refused result with ZERO mutation and no daemon stopped (composes Plan 02). No `pkill -9` anywhere (grep gate in Plan 02 + this plan's verification). |
| T-07-12 | Information Disclosure | doctor invocation echoing a config path into a shell | mitigate | verify calls `doctor._host_capabilities` directly or invokes `doctor.py --json` with a FIXED argv + `json.loads` — resolve-not-execute, no shell interpolation of any path. |
| T-07-13 | Elevation of Privilege | `yulu migrate` running an attacker-controlled "plan" | mitigate | The plan is built in-process by `migrate.plan.build_plan` from the detect result; PlanStep kinds are a fixed enum the apply dispatches on (config_correction/path_route/schema_stamp). There is no externally-supplied plan file and no arbitrary-step execution. |
| T-07-SC | Tampering | npm/pip/cargo installs | accept | This phase performs NO package-manager installs (migration moves/edits the user's own tree only). No `[ASSUMED]`/`[SUS]` package surface -> no install checkpoint needed. (Recorded for register completeness.) |
</threat_model>

<verification>
- `make pytest` green (full suite).
- `tests/test_migrate_apply_rollback.py`: backup-first, rollback byte-for-byte, prune-only-on-verify-pass (both branches), no-data-loss, recording-guard refusal (zero mutation).
- `tests/test_migrate_corrections.py`: mlx_python dropped, ~/Movies/Yulu routed (custom output_dir untouched), schema_version stamped, source PRESERVED.
- `grep -rv '^[[:space:]]*#' yulu/scripts/migrate/ | grep -c pkill` == 0 (no pkill anywhere in the migrate module).
- `bash -n yulu/scripts/yulu` passes; `yulu migrate --dry-run` on a non-existent tree degrades cleanly (no crash, no mutation).
- The human-verify checkpoint confirms the two live-install behaviors (real upgrade no-data-loss/no-reconfig/source-preserved/backup-pruned; live-recording refusal) — NOT faked.
</verification>

<success_criteria>
- MIG-01: `yulu migrate` runs detect->plan->apply->verify with no data loss and no reconfiguration (fixture-proven + live-confirmed).
- MIG-02: apply refuses to stop a daemon while recording; no `pkill -9` (composes Plan 02).
- MIG-03: transactional with `yulu rollback`; the backup is pruned only after verify passes, retained on failure.
- D-04: the dead `mlx_python` field and hardcoded `~/Movies/Yulu` are corrected in transit and the install carries a `schema_version` stamp — with `source` preserved.
</success_criteria>

<output>
Create `.planning/phases/07-seamless-auto-migration/07-03-SUMMARY.md` when done.
</output>
