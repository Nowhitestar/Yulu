---
phase: 03-host-capability-detection-spine
plan: 03
type: execute
wave: 3
depends_on: ["03-01", "03-02"]
files_modified:
  - yulu/scripts/doctor.py
  - tests/test_doctor_host_capabilities.py
autonomous: true
requirements: [DETECT-01, DETECT-03, DETECT-05]
must_haves:
  truths:
    - "Running `yulu doctor --json` emits a `host_capabilities` section with `schema_version` and per-capability provenance + tri-state status, assembled by calling the Plan 01 probes (claude/whisper-cli/mlx-whisper/llm.command/models/recording-dir) and aggregating the Plan 02 default_providers() agent-config entries — without breaking the existing report shape (DETECT-01/03/05, success criteria 1+3+5)"
    - "The §5d source-vs-runtime root bug is fixed: check_yulu_ui receives the RUNTIME root (not the source checkout), so a production install where source_root != runtime_root reports the installed UI honestly (D-07, CONCERNS §5d)"
    - "doctor.py's existing top-level keys (source_root, checks, stt_daemon, search_index, yulu_ui, …) are unchanged — host_capabilities is purely additive and the new probe assembly never raises (the doctor 'check functions never raise — return a dict with error' contract is preserved)"
  artifacts:
    - path: "yulu/scripts/doctor.py"
      provides: "collect_report() extended with a host_capabilities section + the §5d runtime-root fix for check_yulu_ui"
      contains: "host_capabilities"
    - path: "tests/test_doctor_host_capabilities.py"
      provides: "doctor --json host_capabilities coverage + runtime-root + existing-shape-intact Wave-0 assertions"
      contains: "host_capabilities"
  key_links:
    - from: "yulu/scripts/doctor.py collect_report()"
      to: "capabilities.probes + capabilities.provider.default_providers"
      via: "assembles a HostCapabilityReport from the probes and aggregates provider entries"
      pattern: "host_capabilities"
    - from: "yulu/scripts/doctor.py collect_report()"
      to: "check_yulu_ui(runtime_root / 'yulu' / 'scripts', ...)"
      via: "§5d fix — pass runtime_root, not source_root"
      pattern: "runtime_root / \"yulu\""
---

<objective>
Wire the `capabilities/` module into `doctor.py`: `collect_report()` gains a `host_capabilities` section that assembles a versioned `HostCapabilityReport` by calling Plan 01's probes (claude, whisper-cli, mlx-whisper, `llm.command` validity, models, recording-dir) and aggregating Plan 02's `default_providers()` agent-config entries — serialized via `to_dict()` so `yulu doctor --json` carries provenance + tri-state status. Also fix the CONCERNS §5d source-vs-runtime root bug (pass `runtime_root`, not `source_root`, to `check_yulu_ui`).

This is the final Phase 3 plan (Wave 3): it depends on Plan 01 (report + probes) and Plan 02 (provider). The hard constraint: `host_capabilities` is **purely additive** — every existing top-level doctor key stays, and the assembly never raises (the doctor "check functions never raise — return a dict with an error key" contract). Implements DETECT-01/03 (the report surfaces in doctor) and DETECT-05 (the provider's entries reach the report end-to-end).

Purpose: make success criterion 1 ("Running `yulu doctor` emits a versioned HostCapabilityReport JSON") literally true, with the full DETECT-03 coverage and the §5d honesty fix that four downstream consumers depend on.
Output: an extended `doctor.py` and the Wave-0 doctor-integration test.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-host-capability-detection-spine/03-CONTEXT.md
@.planning/phases/03-host-capability-detection-spine/03-VALIDATION.md
@.planning/phases/03-host-capability-detection-spine/03-01-PLAN.md
@.planning/phases/03-host-capability-detection-spine/03-02-PLAN.md
@.planning/codebase/CONCERNS.md
@yulu/scripts/doctor.py
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: doctor.py host_capabilities assembly + §5d runtime-root fix, with Wave-0 doctor-integration test (DETECT-01/03/05, D-05/D-07)</name>
  <files>yulu/scripts/doctor.py, tests/test_doctor_host_capabilities.py</files>
  <read_first>
    - yulu/scripts/doctor.py:272-326 (collect_report — the exact dict shape to EXTEND additively: source_root/runtime_root/config_dir/checks/stt_daemon/search_index/yulu_ui keys; the new `host_capabilities` key goes alongside, NONE removed) and :321 specifically (`"yulu_ui": check_yulu_ui(source_root / "yulu" / "scripts", config_dir)` — the §5d BUG: must become `runtime_root / "yulu" / "scripts"`)
    - yulu/scripts/doctor.py:181-211 (check_search_index — the EXACT lazy-import + "never raises, always returns a dict with an error key" pattern to mirror for the host_capabilities assembly: `sys.path.insert(0, str(Path(__file__).resolve().parent))` then `from capabilities... import ...`, wrapped in try/except returning {"error": ...})
    - yulu/scripts/doctor.py:67-82 (_check_command — the existing host-tool check the new probes COMPLEMENT, not replace; the existing `checks` list stays) and :329-338 (_overall_ok — confirm host_capabilities does NOT need to gate overall ok this phase; leave _overall_ok untouched so an absent optional capability never fails doctor)
    - .planning/phases/03-host-capability-detection-spine/03-01-PLAN.md + 03-02-PLAN.md (the Plan 01 probe functions + the Plan 02 default_providers() the assembly calls — bind to their final SUMMARY-recorded shapes: probes return Capability; HostCapabilityReport.to_dict() yields {schema_version, capabilities})
    - .planning/codebase/CONCERNS.md §5d (check_yulu_ui source-vs-runtime root: production installs where source_root != runtime_root must check the RUNTIME ui dist) and §4d (the host_capabilities gap this section fills)
    - .planning/phases/03-host-capability-detection-spine/03-VALIDATION.md Wave 0 item 4 (test_doctor_host_capabilities.py contract: doctor --json emits a valid host_capabilities covering claude/whisper-cli/mlx-whisper/llm.command/models/recording-dir without breaking the existing report shape; uses runtime_root not source_root)
    - tests/test_doctor.py:1-13,16-37 (the `importlib.util.spec_from_file_location("doctor", DOCTOR)` load idiom + how collect_report is called with explicit source_root/runtime_root/config_dir/legacy_root in tests — reuse this to drive the new section with source_root != runtime_root)
  </read_first>
  <behavior>
    - collect_report() gains a `host_capabilities` key whose value is a HostCapabilityReport.to_dict() — {schema_version, capabilities: {name: {provenance, status, resolved_path, detail}}}. Capability names cover the DETECT-03 set: claude (probe_command), whisper_cli (probe_command), mlx_whisper (probe_mlx_whisper), llm_command (probe_llm_command — resolved-not-executed), models (scan_models), recording_dir (probe_recording_dir). Then it merges the Plan 02 providers: for each `default_providers()`, `provider.capabilities()` entries are added (agent-config provenance) under their keys (e.g. claude_cli, agent_mlx_whisper).
    - The assembly is a single helper (e.g. `_host_capabilities(config_dir, runtime_root) -> dict`) that mirrors check_search_index: lazy `sys.path.insert` + guarded import of `capabilities.probes` / `capabilities.provider`, wrapped in try/except so any failure returns `{"error": str(exc), "schema_version": <n>, "capabilities": {}}` — NEVER raising (doctor contract). It is wired into the collect_report return dict additively.
    - §5d fix: line 321 `check_yulu_ui(source_root / "yulu" / "scripts", config_dir)` becomes `check_yulu_ui(runtime_root / "yulu" / "scripts", config_dir)` — production installs (source_root != runtime_root, e.g. ~/.yulu) now check the runtime UI dist. (When source_root == runtime_root, behavior is unchanged.)
    - NO existing key removed, NO existing key's type changed, _overall_ok untouched (an absent optional capability must not fail doctor — host_capabilities is informational this phase; Phase 5 gates reuse on it).
    - llm_command coverage is resolve-not-execute (inherited from Plan 01's probe_llm_command) — the doctor must never run the configured command (threat T-03-01).
    - Test: load doctor via importlib; call collect_report(source_root=ROOT, runtime_root=<a DIFFERENT tmp_path>, config_dir=<tmp>, legacy_root=<missing>); assert `host_capabilities` is present with an int `schema_version` and a `capabilities` dict; assert the capabilities cover the DETECT-03 names (claude / whisper_cli / mlx_whisper / llm_command / models / recording_dir present as keys, each with provenance ∈ the 4-set + status ∈ the 3-set, NO boolean status); assert EVERY pre-existing top-level key (source_root, runtime_root, config_dir, checks, stt_daemon, search_index, yulu_ui, processes) is still present (shape intact); assert the §5d fix by checking that with source_root != runtime_root, the yulu_ui section was derived from runtime_root (e.g. monkeypatch/spy check_yulu_ui's script_dir arg starts with runtime_root, or assert the rendered path reflects runtime_root). Also assert `doctor.main(["--json", ...])` prints JSON that `json.loads` parses and contains `host_capabilities` (success criterion 1 end-to-end).
  </behavior>
  <action>
    Edit `yulu/scripts/doctor.py`. (1) Add a module-level helper `_host_capabilities(config_dir: Path, runtime_root: Path) -> dict[str, Any]` modeled on check_search_index's lazy-import + never-raise pattern: `sys.path.insert(0, str(Path(__file__).resolve().parent))`, then `from capabilities.probes import probe_command, probe_mlx_whisper, probe_llm_command, scan_models, probe_recording_dir` and `from capabilities.provider import default_providers` and `from capabilities.report import HostCapabilityReport`; build a HostCapabilityReport, populate `report.capabilities` with `claude=probe_command("claude", ("--version",))`, `whisper_cli=probe_command("whisper-cli", ("--version",))`, `mlx_whisper=probe_mlx_whisper()`, `llm_command=probe_llm_command()`, `models=scan_models()`, `recording_dir=probe_recording_dir()`; then for each `p in default_providers()`, merge `p.capabilities()` into `report.capabilities`; `return report.to_dict()`. Wrap the WHOLE body in try/except returning `{"error": str(exc), "schema_version": 1, "capabilities": {}}` so it never raises. (2) In collect_report(), add `"host_capabilities": _host_capabilities(config_dir, runtime_root),` to the returned dict (additive — do not remove or reorder existing keys). (3) Fix §5d: change the `yulu_ui` line from `check_yulu_ui(source_root / "yulu" / "scripts", config_dir)` to `check_yulu_ui(runtime_root / "yulu" / "scripts", config_dir)`. Leave _overall_ok, print_human, and main() otherwise intact (optionally add a one-line host_capabilities summary to print_human, but it must not change exit codes). Create `tests/test_doctor_host_capabilities.py` using the importlib load idiom from tests/test_doctor.py: a test calling `collect_report(source_root=ROOT, runtime_root=tmp_path, config_dir=tmp_path/"cfg", legacy_root=tmp_path/"missing")` asserting host_capabilities is present (schema_version int + capabilities dict), covers the six DETECT-03 capability keys with valid provenance/status and no boolean status, and that all pre-existing top-level keys remain; a test that drives `main(["--json", "--source-root", str(ROOT), "--runtime-root", str(tmp_path), "--config-dir", str(tmp_path), "--legacy-root", str(tmp_path/"missing")])`, captures stdout, json.loads it, and asserts `host_capabilities` is in the parsed object; and a §5d test asserting the yulu_ui section reflects runtime_root not source_root when they differ (spy on check_yulu_ui or assert via a runtime_root-only marker file path).
  </action>
  <verify>
    <automated>cd /Users/liaoyuxing/Documents/Codebase/Yulu/.claude/worktrees/affectionate-mahavira-769bc9 && python3 -m pytest tests/test_doctor_host_capabilities.py tests/test_doctor.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - `python3 -m pytest tests/test_doctor_host_capabilities.py tests/test_doctor.py -x -q` PASSES: the new host_capabilities coverage + runtime-root + existing-shape tests are GREEN AND the pre-existing doctor tests still pass (additive, no regression).
    - `cd yulu/scripts && python3 doctor.py --json --source-root "$(pwd)/../.." --runtime-root "$(pwd)/../.." --config-dir /tmp/yulu-doctor-test | python3 -c "import sys,json; d=json.load(sys.stdin); assert 'host_capabilities' in d; hc=d['host_capabilities']; assert isinstance(hc.get('schema_version'), int); caps=hc['capabilities']; assert {'claude','whisper_cli','mlx_whisper','llm_command','models','recording_dir'} <= set(caps); assert all(c['status'] in ('usable','present-but-unverified','absent') for c in caps.values()); assert all(not isinstance(c['status'], bool) for c in caps.values()); print('OK')"` prints OK (success criterion 1 + DETECT-03 coverage + no-boolean-status, end-to-end via the real CLI).
    - `grep -c 'host_capabilities' yulu/scripts/doctor.py` ≥ 2 (helper + collect_report key) AND `grep -c 'runtime_root / "yulu"' yulu/scripts/doctor.py` ≥ 1 (the §5d fix is in place — `source_root / "yulu"` no longer feeds check_yulu_ui).
    - `grep -c 'source_root / "yulu"' yulu/scripts/doctor.py` == 0 — the §5d bug line is gone (check_yulu_ui no longer receives the source root).
    - The assembly never raises: `python3 -c "import importlib.util,sys; from pathlib import Path; ROOT=Path('.').resolve(); spec=importlib.util.spec_from_file_location('doctor', ROOT/'yulu/scripts/doctor.py'); m=importlib.util.module_from_spec(spec); spec.loader.exec_module(m); r=m.collect_report(source_root=ROOT, runtime_root=Path('/tmp/nope-rt'), config_dir=Path('/tmp/nope-cfg'), legacy_root=Path('/tmp/nope-legacy')); assert 'host_capabilities' in r and all(k in r for k in ('source_root','checks','stt_daemon','yulu_ui')); print('shape-intact')"` prints shape-intact (host_capabilities additive, existing keys preserved, no exception even with missing roots).
    - `python3 -m py_compile yulu/scripts/doctor.py` exits 0.
  </acceptance_criteria>
  <done>`yulu doctor --json` emits a versioned host_capabilities section covering the six DETECT-03 capabilities plus the provider's agent-config entries, never raises, preserves every existing report key, and the §5d source-vs-runtime root bug is fixed; the doctor-integration test and the pre-existing doctor tests are all GREEN.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| config.json → doctor host_capabilities | doctor reads config (recording dir, llm.command) through Plan 01 probes; llm.command head is resolved+statted, NEVER executed |
| capabilities module → doctor | doctor lazily imports capabilities.probes/provider; a broken module degrades to `{"error": ...}`, never crashes doctor |
| doctor --json output → downstream consumers | the serialized host_capabilities (Phase 4 tRPC, Phase 5 reuse) carries only string provenance/status + short detail |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-01 | Elevation/Tampering | doctor's llm_command coverage | mitigate | doctor calls Plan 01's `probe_llm_command`, which RESOLVES+STATS the binary and never executes it — doctor adds no `subprocess.run(llm_command)`. Inherited from Plan 01; the end-to-end acceptance criterion runs the real CLI and asserts the JSON is produced without executing the configured command. |
| T-03-07 | Denial of Service | a probe hanging (subprocess) stalls doctor | mitigate | Plan 01 probes use `subprocess.run(..., timeout=5)`; the host_capabilities helper additionally wraps the whole assembly in try/except → `{"error": ...}`, so a hung/failed probe degrades the section but never hangs or crashes `yulu doctor` (the read-only, never-mutate contract). |
| T-03-04 | Information Disclosure | host_capabilities `detail` in `--json` | mitigate | Entries carry only the short `detail` from the probes (version / "not on PATH" / "not writable"); the assembly never dumps full environment or raw subprocess stderr beyond a first line, and degrades to `{"error": str(exc)}` (ASVS V7 + doctor never-raise). |
| T-03-SC | Tampering | npm/pip/cargo installs | accept | N/A this plan — edits stdlib-only `doctor.py` and imports the stdlib-only `capabilities` module; zero package installs. No `[ASSUMED]`/`[SUS]` packages → no Package Legitimacy checkpoint required. |
</threat_model>

<verification>
- `make pytest` green: `tests/test_doctor_host_capabilities.py` + the pre-existing `tests/test_doctor.py` + the Plan 01/02 capability tests all pass.
- `make test` green before phase verify (full suite + `yulu doctor --json | python3 -m json.tool` valid with `host_capabilities` present — the 03-VALIDATION pre-verify gate).
- `python3 -m py_compile yulu/scripts/doctor.py` clean.
- DETECT-01 (surfaced): `yulu doctor --json` carries a versioned host_capabilities with provenance + tri-state status, no boolean.
- DETECT-03 (complete): the section covers claude / whisper-cli / mlx-whisper / llm.command validity / model paths+sizes / recording-dir writability.
- DETECT-05 (end-to-end): the Plan 02 provider's agent-config entries reach the doctor report via default_providers().
- D-07: the `capabilities/` module integrates into doctor without breaking the existing report shape, and the §5d source-vs-runtime root bug is fixed (runtime_root feeds check_yulu_ui).
- Security: llm.command resolved-not-executed end-to-end (T-03-01); assembly never raises/hangs (T-03-07).
</verification>

<success_criteria>
- DETECT-01 (surfaced): running `yulu doctor` emits the versioned, tri-state, provenance-labeled HostCapabilityReport JSON (success criterion 1).
- DETECT-03: the report covers claude / whisper-cli / mlx-whisper importability / configured llm.command validity / model paths+sizes / recording-dir writability (success criterion 3).
- DETECT-05 (end-to-end): the ClaudeCodeProvider's agent-config entries flow into the report via default_providers() (success criterion 5, completing the provider e2e begun in Plan 02).
- D-07: doctor integrates host_capabilities additively (existing shape intact) and fixes the §5d source-vs-runtime root bug so production installs report honestly.
</success_criteria>

<output>
Create `.planning/phases/03-host-capability-detection-spine/03-03-SUMMARY.md` when done — record the final host_capabilities JSON shape as emitted by `yulu doctor --json` (the capability keys + an example entry), confirm the §5d fix, and note that Phase 4 (tRPC `host_capabilities` endpoint), Phase 5 (reuse gating on tri-state), and Phase 8 (multi-provider aggregation) consume THIS exact shape.
</output>
