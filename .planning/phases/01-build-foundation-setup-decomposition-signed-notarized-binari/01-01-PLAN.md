---
phase: 01-build-foundation-setup-decomposition-signed-notarized-binari
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - yulu/scripts/yulu_platform/__init__.py
  - yulu/scripts/yulu_platform/base.py
  - yulu/scripts/yulu_platform/macos/__init__.py
  - yulu/scripts/yulu_platform/linux/__init__.py
  - yulu/scripts/yulu_platform/windows/__init__.py
  - tests/test_yulu_platform_stubs.py
  - tests/test_yulu_platform_no_shadow.py
autonomous: true
requirements: [BUILD-01]
must_haves:
  truths:
    - "platform/base.py exposes the platform ABCs with linux/ and windows/ arms raising NotImplementedError (SC-5, D-15)"
    - "Importing platform from a process with yulu/scripts on sys.path still resolves to the stdlib platform module, not the yulu_platform package (Pitfall 1, D-15)"
    - "The 4 Python-side ABCs PathResolver, DaemonManager (+ServiceSpec), PermissionModel, DependencyManager exist as interface signatures only (D-16)"
    - "The ABC method signatures carry no leaked macOS vocabulary: no plist keys, no SCStreamConfiguration, no TCC scope strings (D-18)"
    - "The Swift CaptureBackend seam and all macOS implementations are NOT in this package — base.py is Python-only (D-17)"
  artifacts:
    - path: "yulu/scripts/yulu_platform/base.py"
      provides: "Platform-seam ABCs (PathResolver, DaemonManager, PermissionModel, DependencyManager) + ServiceSpec dataclass"
      contains: "class DaemonManager(ABC)"
      min_lines: 40
    - path: "yulu/scripts/yulu_platform/linux/__init__.py"
      provides: "Linux arm concrete stubs that raise NotImplementedError"
      contains: "raise NotImplementedError"
    - path: "yulu/scripts/yulu_platform/windows/__init__.py"
      provides: "Windows arm concrete stubs that raise NotImplementedError"
      contains: "raise NotImplementedError"
    - path: "tests/test_yulu_platform_stubs.py"
      provides: "ABC-uninstantiable + NotImplementedError assertions (SC-5)"
      contains: "NotImplementedError"
    - path: "tests/test_yulu_platform_no_shadow.py"
      provides: "stdlib platform-not-shadowed assertion (Pitfall 1)"
  key_links:
    - from: "yulu/scripts/yulu_platform/linux/__init__.py"
      to: "yulu/scripts/yulu_platform/base.py"
      via: "from yulu_platform.base import DaemonManager, PathResolver, PermissionModel, DependencyManager"
      pattern: "from yulu_platform.base import"
---

<objective>
Stand up the Python-side platform-abstraction skeleton: a `yulu_platform/` package whose `base.py` declares the four platform-seam ABCs as interface signatures only, with `linux/` and `windows/` arms providing concrete subclasses that raise `NotImplementedError`. This satisfies ROADMAP success criterion 5 and is the import target Phase 2 (macOS impls), Phase 3 (`PathResolver` consumer), and Phase 5/7 bind to.

Purpose: Build the cross-platform abstraction NOW (locked project principle) so a future Linux/Windows arm is pure addition, while shipping zero implementation this phase (D-15, D-17).
Output: `yulu_platform/{__init__,base}.py` + `macos/`, `linux/`, `windows/` arms + two pytest files (the Wave 0 scaffolds for SC-5 and the stdlib-shadow guard).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-build-foundation-setup-decomposition-signed-notarized-binari/01-CONTEXT.md
@.planning/phases/01-build-foundation-setup-decomposition-signed-notarized-binari/01-RESEARCH.md
@.planning/phases/01-build-foundation-setup-decomposition-signed-notarized-binari/01-PATTERNS.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Define the yulu_platform package + the four platform-seam ABCs</name>
  <files>yulu/scripts/yulu_platform/__init__.py, yulu/scripts/yulu_platform/base.py, yulu/scripts/yulu_platform/macos/__init__.py, yulu/scripts/yulu_platform/linux/__init__.py, yulu/scripts/yulu_platform/windows/__init__.py</files>
  <read_first>
    - .planning/phases/01-build-foundation-setup-decomposition-signed-notarized-binari/01-RESEARCH.md (Pattern 6 "Python platform-seam ABCs" lines 341-405 — the authoritative spec for the ABC bodies; Pitfall 1 lines 448-459 — the verified stdlib-shadow break; Recommended Project Structure lines 198-203)
    - .planning/phases/01-build-foundation-setup-decomposition-signed-notarized-binari/01-PATTERNS.md (yulu_platform/base.py section lines 391-408; package-init section lines 412-420; linux/windows stub section lines 424-438)
    - yulu/scripts/recording_lock.py (style analog: `from __future__ import annotations`, frozen `@dataclass`, typed class — lines 1-39)
    - yulu/scripts/stt_daemon/config.py (style analog: typed `@dataclass` with field defaults — lines 14-28)
    - yulu/scripts/stt_daemon/__init__.py (single-line module docstring pattern)
  </read_first>
  <behavior>
    - `from yulu_platform.base import DaemonManager` then `DaemonManager()` raises `TypeError` (cannot instantiate an ABC with abstract methods)
    - `from yulu_platform.linux import LinuxDaemonManager` then `LinuxDaemonManager().load("x")` raises `NotImplementedError`
    - Each of the 4 ABCs (PathResolver, DaemonManager, PermissionModel, DependencyManager) is importable from `yulu_platform.base`
    - `ServiceSpec` is a frozen dataclass importable from `yulu_platform.base` with fields name/program/keep_alive/working_dir/environment
  </behavior>
  <action>
Create the package `yulu/scripts/yulu_platform/` — the name is `yulu_platform`, NEVER `platform` (RESEARCH Pitfall 1: a `platform/` package on `yulu/scripts/` shadows stdlib `platform`, which numpy in echo_cancel.py imports; the stt_daemon plist puts `yulu/scripts` on PYTHONPATH — this is a verified break, not a style nit).

`__init__.py`: one-line module docstring (mirror `stt_daemon/__init__.py`): `"""yulu_platform — cross-platform seam interfaces (macOS impls in Phase 2)."""`.

`base.py` (stdlib only — `abc`, `dataclasses`, `pathlib`, `__future__.annotations`; CLAUDE.md mandates stdlib-first, RESEARCH Standard Stack confirms no new deps). Define per RESEARCH Pattern 6:
- `ServiceSpec` — `@dataclass(frozen=True)`: `name: str`, `program: list[str]`, `keep_alive: bool = True`, `working_dir: Path | None = None`, `environment: dict[str, str] | None = None`. Docstring must state "platform-neutral daemon description (NO plist keys — D-18)".
- `DaemonManager(ABC)` — `@abstractmethod` `install(self, spec: ServiceSpec) -> None`, `load(self, name: str) -> None`, `unload(self, name: str) -> None`, `status(self, name: str) -> str`. Grounded in PLAT-03.
- `PathResolver(ABC)` — `@abstractmethod` `config_dir(self) -> Path`, `data_dir(self) -> Path` (replaces hardcoded ~/Movies/Yulu, PLAT-04), `runtime_dir(self) -> Path` (sockets/locks/PIDs, DATA-02 future).
- `PermissionModel(ABC)` — `@abstractmethod` `check(self, capability: str) -> str`. NO TCC vocabulary in the signature (D-18).
- `DependencyManager(ABC)` — `@abstractmethod` `is_available(self, name: str) -> bool`, `install(self, name: str) -> None`. Grounded in PLAT-05.
All abstract method bodies are `...`. D-18: signatures carry NO leaked macOS vocabulary (no plist keys, no `SCStreamConfiguration`, no TCC scope strings) — a reviewer must be able to imagine a systemd arm implementing the same methods.

`macos/__init__.py`: empty placeholder docstring only — D-17 says Phase 2 implements the macOS arm; Phase 1 ships an empty stub dir.

`linux/__init__.py`: `from yulu_platform.base import DaemonManager, PathResolver, PermissionModel, DependencyManager`, then one concrete subclass per ABC (`LinuxDaemonManager`, `LinuxPathResolver`, `LinuxPermissionModel`, `LinuxDependencyManager`) overriding EVERY abstract method with `raise NotImplementedError("<seam> not implemented (v2 XPLAT-01)")`. A subclass that overrides all abstract methods IS instantiable — only the bare ABC raises `TypeError` — so these classes construct fine but every method raises (that asymmetry is what the tests assert).

`windows/__init__.py`: a mirror of `linux/__init__.py` with "Linux"→"Windows" in class names and messages.

D-15: this phase ships interface signatures + raising stubs only — no working implementation. D-16: exactly these 4 ABCs. D-17: NO Swift `CaptureBackend` here, this package is Python-only.
  </action>
  <verify>
    <automated>cd /Users/liaoyuxing/Documents/Codebase/Yulu/.claude/worktrees/affectionate-mahavira-769bc9 && python3 -m py_compile yulu/scripts/yulu_platform/base.py yulu/scripts/yulu_platform/linux/__init__.py yulu/scripts/yulu_platform/windows/__init__.py && PYTHONPATH=yulu/scripts python3 -c "from yulu_platform.base import DaemonManager, PathResolver, PermissionModel, DependencyManager, ServiceSpec; from yulu_platform.linux import LinuxDaemonManager; print('imports ok')"</automated>
  </verify>
  <acceptance_criteria>
    - `PYTHONPATH=yulu/scripts python3 -c "from yulu_platform.base import DaemonManager; DaemonManager()"` raises `TypeError` (bare ABC uninstantiable)
    - `PYTHONPATH=yulu/scripts python3 -c "from yulu_platform.linux import LinuxDaemonManager; LinuxDaemonManager().load('x')"` raises `NotImplementedError`
    - `grep -E "SCStreamConfiguration|plist|tccutil|ScreenCapture|com.apple" yulu/scripts/yulu_platform/base.py` returns no matches (D-18: no leaked macOS vocabulary)
    - `ls yulu/scripts/yulu_platform/macos/__init__.py` exists (Phase 2 placeholder)
    - `grep -c "@abstractmethod" yulu/scripts/yulu_platform/base.py` >= 10 (4 ABCs covering all required methods)
  </acceptance_criteria>
  <done>The yulu_platform package exists with base.py declaring the 4 ABCs + ServiceSpec, macos/ empty stub, and linux/windows arms whose every method raises NotImplementedError; all files py_compile and import cleanly with yulu/scripts on PYTHONPATH.</done>
</task>

<task type="auto">
  <name>Task 2: Wave 0 test scaffolds — ABC stubs + stdlib-shadow guard</name>
  <files>tests/test_yulu_platform_stubs.py, tests/test_yulu_platform_no_shadow.py</files>
  <read_first>
    - .planning/phases/01-build-foundation-setup-decomposition-signed-notarized-binari/01-RESEARCH.md (Code Examples lines 505-520 — the exact test spec for the stub assertions; Validation Architecture Wave 0 Gaps lines 617-619)
    - .planning/phases/01-build-foundation-setup-decomposition-signed-notarized-binari/01-PATTERNS.md (test sections lines 456-462; Test Root Anchor pattern lines 505-507)
    - .planning/phases/01-build-foundation-setup-decomposition-signed-notarized-binari/01-VALIDATION.md (Wave 0 Requirements — the no-shadow + stubs requirement)
    - tests/conftest.py (confirm it only registers markers — new tests are plain functions, no fixtures)
    - tests/test_dev_install.py (analog: how existing tests put yulu/scripts on sys.path)
  </read_first>
  <behavior>
    - test_base_is_abstract: `DaemonManager()` raises `TypeError`
    - test_linux_arm_raises: `LinuxDaemonManager().load(...)` raises `NotImplementedError` (and same for one method of each other linux stub)
    - test_no_stdlib_shadow: with `yulu/scripts` on `sys.path`, `import platform; platform.__file__` resolves OUTSIDE `yulu/scripts` (i.e. to the stdlib path), proving `yulu_platform` does not shadow stdlib `platform`
  </behavior>
  <action>
Create both pytest files using the repo convention `ROOT = Path(__file__).resolve().parents[1]` (every existing test file uses it) and `sys.path.insert(0, str(ROOT / "yulu" / "scripts"))` so `yulu_platform` is importable.

`tests/test_yulu_platform_stubs.py` (RESEARCH Code Examples is the spec) — covers SC-5:
- `test_base_is_abstract`: `from yulu_platform.base import DaemonManager`; `with pytest.raises(TypeError): DaemonManager()`.
- `test_linux_daemon_manager_is_stub`: `from yulu_platform.linux import LinuxDaemonManager`; `with pytest.raises(NotImplementedError): LinuxDaemonManager().load("com.yulu.sttdaemon")`.
- Add one analogous `NotImplementedError` assertion per remaining linux stub (PathResolver.config_dir, PermissionModel.check, DependencyManager.is_available) and a mirror windows assertion for at least DaemonManager, so the whole seam is proven raising.

`tests/test_yulu_platform_no_shadow.py` (RESEARCH Pitfall 1 verification, static-style assert) — covers the §1 shadow guard:
- With `ROOT / "yulu" / "scripts"` on `sys.path`, `import platform`, then assert `Path(platform.__file__).resolve()` is NOT under `ROOT / "yulu" / "scripts"` (i.e. it is the genuine stdlib module). This fails loudly if anyone ever renames the package back to `platform/`.

Both files are plain pytest functions (conftest only registers e2e/integration markers — no fixtures to inherit).
  </action>
  <verify>
    <automated>cd /Users/liaoyuxing/Documents/Codebase/Yulu/.claude/worktrees/affectionate-mahavira-769bc9 && python3 -m pytest tests/test_yulu_platform_stubs.py tests/test_yulu_platform_no_shadow.py -q</automated>
  </verify>
  <acceptance_criteria>
    - `python3 -m pytest tests/test_yulu_platform_stubs.py tests/test_yulu_platform_no_shadow.py -q` exits 0 with all tests passing
    - `test_yulu_platform_no_shadow.py` asserts `platform.__file__` is outside `yulu/scripts` (would FAIL if the package were named `platform/`)
    - `grep -c "NotImplementedError" tests/test_yulu_platform_stubs.py` >= 4 (one assertion per linux stub + a windows mirror)
    - Both files use `ROOT = Path(__file__).resolve().parents[1]`
  </acceptance_criteria>
  <done>Both Wave 0 test files exist and pass: the stubs test proves the bare ABC is uninstantiable and every linux/windows method raises NotImplementedError (SC-5); the no-shadow test proves stdlib `platform` is not shadowed (Pitfall 1).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Python import resolution (`sys.path`) | The stt_daemon plist puts `yulu/scripts` on PYTHONPATH; any package there can shadow a stdlib module imported by first-party or third-party code. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-01 | Tampering | `yulu_platform` package name vs stdlib `platform` | mitigate | Name the package `yulu_platform`; `test_yulu_platform_no_shadow.py` asserts `platform.__file__` resolves to stdlib — a regression (rename back to `platform/`) fails CI. RESEARCH Pitfall 1 proved numpy (echo_cancel.py) breaks otherwise. |
| T-01-02 | Denial of Service | linux/windows stubs called in production by mistake | accept | Stubs raise `NotImplementedError` with an explicit "v2 XPLAT-01" message — fail-loud, not silent; macOS arm is the only one wired this milestone, so no runtime path reaches these. Low risk. |
| T-01-SC | Tampering | npm/pip/cargo installs | mitigate | No package installs in this plan (stdlib only). No `[ASSUMED]`/`[SUS]` packages to gate. |
</threat_model>

<verification>
- `python3 -m pytest tests/test_yulu_platform_stubs.py tests/test_yulu_platform_no_shadow.py -q` passes (SC-5 + Pitfall 1 guard).
- `make pytest` stays green (no regression to existing suite).
- `grep` confirms base.py carries no macOS vocabulary (D-18).
</verification>

<success_criteria>
- ROADMAP SC-5 met: `yulu_platform/base.py` exposes the 4 ABCs; linux/ and windows/ arms raise NotImplementedError.
- D-15/D-16/D-17/D-18 all satisfied: interface-only, 4 named ABCs, Python-only, no macOS vocabulary.
- The stdlib-shadow landmine (Pitfall 1) is permanently guarded by a passing test.
</success_criteria>

<output>
Create `.planning/phases/01-build-foundation-setup-decomposition-signed-notarized-binari/01-01-SUMMARY.md` when done.
</output>
