---
phase: 10-provider-model-xai-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - yulu/scripts/yulu_ui/src/config.ts
  - yulu/scripts/yulu_ui/src/settingsRegistry.ts
  - yulu/scripts/config.example.json
  - yulu/scripts/yulu_ui/src/hostStore.ts
  - yulu/scripts/yulu_ui/src/agentSessionStore.ts
  - yulu/scripts/yulu_ui/tests/config.test.ts
  - yulu/scripts/yulu_ui/tests/settingsRegistry.test.ts
  - yulu/scripts/yulu_ui/tests/hostStore.test.ts
  - yulu/scripts/yulu_ui/tests/agentSessionStore.test.ts
autonomous: true
requirements: [PRVD-01, PRVD-02, PRVD-03, XAI-04]

must_haves:
  truths:
    - "Transcription, summary, and conversation selections persist independently and no secret-shaped value enters config."
    - "A summary task stores the resolved provider and exact model at enqueue time; claiming/retrying never overwrites either field."
    - "An ask session stores its provider/model locally at creation and can enter a durable paused state without losing messages."
    - "Old config, task databases, and session files migrate to explicit backward-compatible defaults instead of becoming unreadable."
  artifacts:
    - path: "yulu/scripts/yulu_ui/src/config.ts"
      provides: "Capability-specific summary/conversation selection schema"
      contains: "summary"
    - path: "yulu/scripts/yulu_ui/src/hostStore.ts"
      provides: "Immutable summary provider/model columns and provider-paused task state"
      contains: "summary_provider"
    - path: "yulu/scripts/yulu_ui/src/agentSessionStore.ts"
      provides: "Local conversation provider/model/status snapshot"
      contains: "provider"
  key_links:
    - from: "config.ts capability selection"
      to: "HostStore.enqueueRecording"
      via: "recording pipeline resolves once before enqueue; later plans consume stored identity"
      pattern: "summaryProvider|summary_provider"
    - from: "agentSessionStore create"
      to: "routers/ask.ts"
      via: "later ask dispatch reads session snapshot, not current config"
      pattern: "provider.*model"
---

<objective>
Create the durable provider identity that every later Phase 10 request can trust.

Purpose: Fix the current root violation where `claimNext(provider)` rewrites the task's Agent provider at dispatch time, and establish independent capability choices without building runtime/gateway connection UX.
Output: Backward-compatible config, SQLite, and local-session schemas with immutable creation-time provider/model snapshots and explicit paused states.
</objective>

<execution_context>
@/Users/liaoyuxing/.codex/get-shit-done/workflows/execute-plan.md
@/Users/liaoyuxing/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/10-provider-model-xai-foundation/10-CONTEXT.md
@.planning/phases/10-provider-model-xai-foundation/10-RESEARCH.md
@.planning/phases/10-provider-model-xai-foundation/10-PATTERNS.md
@.planning/phases/10-provider-model-xai-foundation/10-VALIDATION.md

@yulu/scripts/yulu_ui/src/config.ts
@yulu/scripts/yulu_ui/src/settingsRegistry.ts
@yulu/scripts/yulu_ui/src/hostStore.ts
@yulu/scripts/yulu_ui/src/agentSessionStore.ts

<interfaces>
- Keep `transcription.engine: "local" | "xai"` as the existing independent transcription selector.
- Preserve `llm.agent.provider` as the local Agent runtime selection consumed by Phase 12; Phase 10 may resolve it to a task/session identity but must not own its OAuth.
- Preserve `HostStore`'s transaction/lease/event pattern and `agent_tasks` table in place; use additive `ALTER TABLE` migration.
- Preserve local `agent-sessions.json` and atomic temp+rename writes; migrate parsed v1 sessions by Zod defaults rather than a second store.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add independent summary and conversation selection without credential fields</name>
  <files>yulu/scripts/yulu_ui/src/config.ts, yulu/scripts/yulu_ui/src/settingsRegistry.ts, yulu/scripts/config.example.json, yulu/scripts/yulu_ui/tests/config.test.ts, yulu/scripts/yulu_ui/tests/settingsRegistry.test.ts</files>
  <read_first>
    - `config.ts` `ConfigSchema`, passthrough/default behavior, and `ConfigManager.update` — retain partial-config compatibility and atomic persistence.
    - `settingsRegistry.ts` longest-prefix lookup and hidden `llm.agent.provider` entry — add only the two settings that Phase 10 exposes later.
    - `.planning/phases/10-provider-model-xai-foundation/10-CONTEXT.md` D-01, D-04, D-05, D-10 — independent choices, no config secret, runtime-managed Agent model.
  </read_first>
  <behavior>
    - A minimal/legacy config parses to summary provider `agent`, conversation provider `agent`, and a documented exact xAI text model default; existing transcription remains `local` by default.
    - Summary and conversation each contain only provider/model selection. Updating one path leaves the other capability and transcription unchanged.
    - Allowed Phase 10 text providers are `agent` and `xai`; do not add nonfunctional direct/gateway/runtime choices before Phase 12.
    - Agent selection exposes model identity as `runtime-managed`; xAI requires a non-empty bounded model string. No key/token/secret/password/credential field exists anywhere under active config.
    - `config.example.json` describes the local-default behavior and makes no Hermes requirement.
  </behavior>
  <action>
    Add failing config/registry tests first. Extend `ConfigSchema` with the smallest `intelligence` object containing `summary` and `conversation` selections; keep Zod defaults/passthrough behavior so old files parse without a rewrite. Register the two provider/model setting paths under the existing `llm` category with reload `none`; leave Agent runtime connection fields hidden for Phase 12. Update the example config only where needed.

    Add a negative source/object assertion that serializable config contains no secret-shaped provider credential key. Do not build a provider registry, gateway schema, onboarding flow, or model-discovery cache in this task.
  </action>
  <verify>
    <automated>cd yulu/scripts/yulu_ui &amp;&amp; npm test -- --run tests/config.test.ts tests/settingsRegistry.test.ts</automated>
  </verify>
  <done>All three capabilities have independent, backward-compatible selection state and active config contains no credential surface.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Snapshot summary provider/model in the durable task and stop claim-time rebinding</name>
  <files>yulu/scripts/yulu_ui/src/hostStore.ts, yulu/scripts/yulu_ui/tests/hostStore.test.ts</files>
  <read_first>
    - `hostStore.ts:1-150,190-260,400-470,990-1090` — task types, enqueue, claim, pause/release, and additive migration.
    - Existing `tests/hostStore.test.ts` concurrent claim, recovery, retry, and migration cases — extend these tests; do not create a second task store.
    - Research Threat T-10-01 and T-10-07 — claim must never overwrite identity; paused work must not auto-spend/retry.
  </read_first>
  <behavior>
    - `AgentTask` exposes `summaryProvider` and `summaryModel`; enqueue requires resolved values and records them in the queued event.
    - New SQLite columns are additive and non-null. Legacy rows derive summary provider from their existing `agent_provider` and model `runtime-managed` without losing task/artifact rows.
    - `claimNext()` and `claim(id)` no longer accept/write a provider. They lease the already-pinned row and record the pinned identity in `task.claimed`.
    - A generalized `awaiting_provider` state releases the lease, retains a committed transcript, stores a bounded safe error, and is excluded from automatic dispatch until explicit same-provider retry.
    - `retry(id)` retains summary provider/model. No task method mutates these columns after insert.
  </behavior>
  <action>
    Write migration and immutability tests first, including a legacy database fixture, claim after a changed provider argument (which should no longer exist), provider pause after transcript commit, and retry retaining exact values.

    Add the two columns and state to the existing types/table migration. Remove provider parameters and `agent_provider = ?` writes from claim methods. Keep `agent_provider` for the independently pinned Agent runtime/delivery compatibility; do not rename the table or rewrite historical rows. Add the smallest pause method/event needed by Plans 10-03 and 10-04. Update active/deletion/recovery state sets consistently.
  </action>
  <verify>
    <automated>cd yulu/scripts/yulu_ui &amp;&amp; npm test -- --run tests/hostStore.test.ts</automated>
  </verify>
  <done>Every task retains creation-time summary provider/model across claim, restart, failure, and retry, with an explicit non-dispatching provider pause.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Extend the local conversation session with provider/model and pause metadata</name>
  <files>yulu/scripts/yulu_ui/src/agentSessionStore.ts, yulu/scripts/yulu_ui/tests/agentSessionStore.test.ts</files>
  <read_first>
    - `agentSessionStore.ts` schemas, `createAgentSession`, append/update helpers, and atomic store writer.
    - `routers/agentSessions.ts` current consumers — keep existing background Agent sessions readable; routing changes land in Plan 10-04.
    - Context D-02, D-03, D-09 — local immutable identity/history and paused behavior.
  </read_first>
  <behavior>
    - Ask-purpose sessions persist `provider`, `model`, `status: active|paused`, and optional bounded `pausedReason`; existing `agent` remains as a backward-compatible projection.
    - Old session JSON parses with provider from `agent`, model `runtime-managed`, and active status; messages/sources/pins/archive/native session IDs are preserved.
    - Creating an ask session requires provider/model. Background Agent Console sessions keep their existing behavior and do not accidentally become xAI conversations.
    - `pauseAgentSession` and `resumeAgentSession` mutate only status/reason; they never change provider/model or erase history.
    - Store writes remain local, atomic, and contain no OAuth/API credentials.
  </behavior>
  <action>
    Add a focused store test file first for legacy parsing, creation snapshot, pause/resume, append while active, and identity immutability. Extend the existing Zod schema and helpers in place. Do not introduce SQLite or a second history file. Keep message size/source schemas and current UI summary shape compatible, adding provider/model/status fields only.
  </action>
  <verify>
    <automated>cd yulu/scripts/yulu_ui &amp;&amp; npm test -- --run tests/agentSessionStore.test.ts tests/routers/agentSessions.test.ts</automated>
  </verify>
  <done>Conversation identity and failure state are durable local session data, and legacy/background sessions remain readable.</done>
</task>

</tasks>

<threat_model>
- **T-10-01 (High):** claim-time provider/model substitution — blocked by insert-only identity columns and immutability tests.
- **T-10-02 (High):** credential persistence in config/session/task — blocked by schema/source gates and absence of secret fields.
- **T-10-07 (High):** automatic retry/fallback after failure — blocked by non-dispatching `awaiting_provider` and explicit retry.
</threat_model>

<source_coverage>
| Source | Items | Coverage |
|--------|-------|----------|
| GOAL/ROADMAP | Independent choices, pinned work, explicit pause | Tasks 1-3 |
| REQUIREMENTS | PRVD-01, PRVD-02, PRVD-03, XAI-04 local history | Tasks 1-3 |
| CONTEXT | D-01, D-02, D-03, D-04, D-05, D-09, D-10 | Tasks 1-3; credentials/runtime UX intentionally deferred |
| RESEARCH | Creation-time resolution; claim overwrite root cause; provider failure as state | Tasks 2-3 |
| PATTERNS | ConfigManager, HostStore, agentSessionStore | Directly reused; no replacement store |
</source_coverage>

<verification>
Run all three focused test groups, then `cd yulu/scripts/yulu_ui &amp;&amp; npm run typecheck`. Inspect the SQLite migration against both a new DB and a legacy fixture. Source-audit every write to `summary_provider`, `summary_model`, and session provider/model; only creation/migration may assign identity.
</verification>

<success_criteria>
- PRVD-01 has an independent persisted selection contract.
- PRVD-02 has immutable summary/conversation identity at the durable boundary.
- PRVD-03 has a durable pause state with no dispatcher eligibility.
- No Phase 12 runtime/gateway connection behavior or credential field is introduced.
</success_criteria>

<output>
Create `.planning/phases/10-provider-model-xai-foundation/10-01-SUMMARY.md` when done.
</output>
