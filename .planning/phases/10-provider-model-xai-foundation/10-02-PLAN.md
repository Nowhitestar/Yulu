---
phase: 10-provider-model-xai-foundation
plan: 02
type: execute
wave: 2
depends_on: [10-01]
files_modified:
  - yulu/scripts/xai_keychain.swift
  - yulu/scripts/yulu_ui/src/xaiCredentials.ts
  - yulu/scripts/yulu_ui/src/xaiText.ts
  - yulu/scripts/yulu_ui/src/routers/providers.ts
  - yulu/scripts/yulu_ui/src/routers/_app.ts
  - yulu/scripts/yulu_ui/src/trpc.ts
  - yulu/scripts/yulu_ui/src/server.ts
  - yulu/scripts/yulu_ui/tests/xaiCredentials.test.ts
  - yulu/scripts/yulu_ui/tests/xaiText.test.ts
  - yulu/scripts/yulu_ui/tests/routers/providers.test.ts
  - tests/test_xai_keychain_source.py
autonomous: true
requirements: [PRVD-04, PRVD-05, XAI-01, XAI-04]

must_haves:
  truths:
    - "One xAI OAuth credential is reused by STT, summary, and conversation; an explicit xAI API key may be used only as a Keychain-backed fallback."
    - "Direct/gateway secret slots use the existing packaged helper and never place the secret in argv, environment, config, SQLite, logs, or browser read-back."
    - "Every xAI text request uses the exact requested model, `store:false`, no tools, bounded input/output, and the fixed xAI HTTPS origin."
    - "Transcription, summary, and conversation have separate factual readiness results based on real capability requests."
  artifacts:
    - path: "yulu/scripts/xai_keychain.swift"
      provides: "Backward-compatible validated Keychain slots"
      contains: "com.yulu"
    - path: "yulu/scripts/yulu_ui/src/xaiText.ts"
      provides: "Strict Responses API summary/conversation client"
      contains: "store: false"
    - path: "yulu/scripts/yulu_ui/src/routers/providers.ts"
      provides: "Status, credential write/clear, and capability-specific probe API"
      contains: "conversation"
  key_links:
    - from: "XaiTextClient"
      to: "XaiCredentialManager"
      via: "same resolved OAuth/API-key bearer source as XaiAudioClient"
      pattern: "resolve"
    - from: "providers router"
      to: "xAI audio/text clients"
      via: "separate real probes; status never promotes one capability from another probe"
      pattern: "transcription|summary|conversation"
---

<objective>
Add the minimum secure direct-xAI text and credential foundation used by summaries and conversation.

Purpose: Reuse the shipped OAuth/Keychain/audio path, close the API-key/gateway secret custody requirement, and establish request-level privacy/readiness behavior before integrating production features.
Output: Backward-compatible multi-slot Keychain helper, shared xAI credential resolution, strict Responses client, and separate provider probe API.
</objective>

<execution_context>
@/Users/liaoyuxing/.codex/get-shit-done/workflows/execute-plan.md
@/Users/liaoyuxing/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/10-provider-model-xai-foundation/10-CONTEXT.md
@.planning/phases/10-provider-model-xai-foundation/10-RESEARCH.md
@.planning/phases/10-provider-model-xai-foundation/10-PATTERNS.md
@.planning/phases/10-provider-model-xai-foundation/10-VALIDATION.md
@yulu/spec/adr/008-yulu-owned-xai-oauth.md

@yulu/scripts/xai_keychain.swift
@yulu/scripts/yulu_ui/src/xaiCredentials.ts
@yulu/scripts/yulu_ui/src/xaiAudio.ts
@yulu/scripts/yulu_ui/src/routers/xaiAudio.ts
@yulu/scripts/yulu_ui/src/server.ts

<interfaces>
- Preserve `xai_keychain read|write|delete` with no slot as the existing `com.yulu.xai-oauth/default` item.
- Preserve `XaiCredentialManager.status/authorize/resolve/logout`; extend `resolve` metadata without exposing tokens to routers/UI.
- Use native `fetch`, AbortController, Zod/manual object checks, and existing tRPC. Add no xAI SDK, retry library, or credential package.
- Summary/conversation probes call the same `XaiTextClient` public methods later used in production; they are not command-presence checks.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Generalize the packaged Keychain helper and add explicit xAI API-key fallback</name>
  <files>yulu/scripts/xai_keychain.swift, yulu/scripts/yulu_ui/src/xaiCredentials.ts, yulu/scripts/yulu_ui/tests/xaiCredentials.test.ts, tests/test_xai_keychain_source.py</files>
  <read_first>
    - `xai_keychain.swift` fixed service/account, stdin JSON validation, 65 KiB cap, device-only accessibility, and exit 44 behavior.
    - `xaiCredentials.ts:1-220` helper runner and credential parsing; secrets are already stdin/stdout, not argv.
    - ADR-008 token/origin/logging boundary and Context D-04/D-12.
  </read_first>
  <behavior>
    - No-slot helper calls retain byte-for-byte OAuth item compatibility.
    - An optional slot supports `direct.<safe-id>` and `gateway.<safe-id>` under a separate provider-secret Keychain service/account; reject any value outside a strict bounded ASCII allowlist before Security.framework calls.
    - Secret payload remains a bounded JSON object over stdin; the secret is never a command argument or environment variable. Read/delete keep exit 44 semantics.
    - Host code can set/resolve/clear a secret slot and ask only whether it exists. Browser-facing status never contains the secret or OAuth token.
    - xAI credential resolution prefers a valid OAuth token. Only when no OAuth credential exists may the explicit `direct.xai` API key be used; auth source is reported as `oauth|api-key` without the value. An OAuth service error does not silently downgrade to the key.
    - Replace the temporary public Grok CLI client ID only with the Yulu-owned registered client ID supplied for execution; a source test rejects the temporary ID and empty placeholder. Never log either credential.
  </behavior>
  <action>
    Extend the current helper rather than creating another binary. Add source/unit tests first for slot validation, legacy invocation, stdin transport, no-secret argv/env/log surface, OAuth priority, explicit key fallback, and no fallback on OAuth refresh failure. Reuse the existing child-process runner; expose only the minimum internal store needed by the provider router. Keep generic gateway slot storage dormant until Phase 12 consumes it—no connection schema or execution path here.
  </action>
  <verify>
    <automated>cd yulu/scripts/yulu_ui &amp;&amp; npm test -- --run tests/xaiCredentials.test.ts &amp;&amp; cd ../../.. &amp;&amp; python3 -m pytest -q tests/test_xai_keychain_source.py</automated>
  </verify>
  <done>OAuth remains the shared primary xAI credential, API-key fallback and future gateway slots are Keychain-only, and the temporary client registration cannot pass the source gate.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement one strict xAI Responses client for summary and conversation</name>
  <files>yulu/scripts/yulu_ui/src/xaiText.ts, yulu/scripts/yulu_ui/tests/xaiText.test.ts</files>
  <read_first>
    - `xaiAudio.ts` constants, injected fetch, timeout/retry/error patterns, fixed STT URL, and response guards.
    - Official xAI Generate Text/Responses/Models/Security docs cited in `10-RESEARCH.md`.
    - Context D-07 through D-10 and Threats T-10-03 through T-10-08.
  </read_first>
  <behavior>
    - `summarize` and `answer` accept an exact non-empty bounded model, already-bounded inputs, and use `POST https://api.x.ai/v1/responses`.
    - Body always includes `store:false`; it never includes `tools`, `previous_response_id`, files, collections, Web Search, or X Search.
    - Authorization comes only from credential manager resolution. URL is a fixed xAI HTTPS constant; redirects to non-xAI origins are rejected or disabled.
    - Summary prompt contains instructions and transcript; conversation prompt contains question, bounded local history, and bounded numbered excerpts. The client itself performs no file/search/connector access.
    - Parse the documented textual output shape into non-empty bounded UTF-8 text. Reject malformed/oversized/error responses with safe messages that omit body, prompt, token, and secret.
    - No silent model/provider retry. A narrowly bounded transient retry, if retained from `XaiAudioClient`, must use the same exact provider/model/request and expose one final failure.
  </behavior>
  <action>
    Write injected-fetch tests first that assert the full outbound URL/headers/body, including negative absence of forbidden fields and redacted error behavior. Implement a single file beside `xaiAudio.ts` using native fetch and the existing credential interface. Keep summary and answer as two explicit methods sharing only private request/response parsing; do not add a generic provider framework or xAI SDK.
  </action>
  <verify>
    <automated>cd yulu/scripts/yulu_ui &amp;&amp; npm test -- --run tests/xaiText.test.ts</automated>
  </verify>
  <done>A production-usable xAI text client enforces exact model, request privacy, bounded data, and safe response handling for both capabilities.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Expose one provider status surface with three independent real probes</name>
  <files>yulu/scripts/yulu_ui/src/routers/providers.ts, yulu/scripts/yulu_ui/src/routers/_app.ts, yulu/scripts/yulu_ui/src/trpc.ts, yulu/scripts/yulu_ui/src/server.ts, yulu/scripts/yulu_ui/tests/routers/providers.test.ts</files>
  <read_first>
    - `routers/xaiAudio.ts` status/authorize/cancel/logout/test procedures and mutation invalidation expectations.
    - `server.ts` one-instance construction of credentials/audio clients and `AppContext` wiring.
    - UI-SPEC State Matrix — each capability status is factual and independent.
  </read_first>
  <behavior>
    - One router returns shared xAI connection/auth-source status plus separate `untested|testing|ready|failed` transcription, summary, and conversation results with tested model/time and safe detail.
    - Existing authorize/cancel/logout behavior remains compatible; logout clears OAuth only, while API-key removal is an explicit separate action.
    - API-key set accepts a bounded secret mutation, writes it directly to Keychain, clears request-local input, and returns status only. There is no read procedure.
    - Transcription probe reuses the current real WAV probe. Summary and conversation probes use the production `XaiTextClient` methods with small synthetic content, exact configured model, `store:false`, and output validation.
    - One probe's success never marks another capability ready. Changing its configured model invalidates that capability's prior ready projection.
  </behavior>
  <action>
    Add router tests first with fake credential/audio/text clients and exact call counts. Construct one `XaiTextClient` beside the existing `XaiAudioClient`, add typed optional context fields for tests, register the router, and preserve the old xAI audio router while UI migrates in Plan 10-05. Keep probe state in host memory; readiness must not be confused with durable provider selection and need not add a database.
  </action>
  <verify>
    <automated>cd yulu/scripts/yulu_ui &amp;&amp; npm test -- --run tests/routers/providers.test.ts tests/routers/xaiAudio.test.ts &amp;&amp; npm run typecheck</automated>
  </verify>
  <done>The host exposes one shared xAI connection and three independent capability-specific real-request readiness proofs without secret read-back.</done>
</task>

</tasks>

<threat_model>
- **T-10-02 (High):** secret disclosure — stdin-only helper, strict slot allowlist, status-only API, negative source/request tests.
- **T-10-03 (High):** bearer token exfiltration — fixed xAI HTTPS origin and validated OAuth discovery endpoints.
- **T-10-05 (High):** remote persistence/tool access — request-body gates require `store:false` and forbid tools/state/files.
- **T-10-07 (High):** hidden spend/fallback — exact provider/model, at most same-request transient retry, no capability promotion by another probe.
- **T-10-09 (High):** non-Yulu OAuth client — source/package gate blocks the temporary client ID before final checkpoint.
</threat_model>

<source_coverage>
| Source | Items | Coverage |
|--------|-------|----------|
| GOAL/ROADMAP | Explicit custody, one xAI connection, three real probes | Tasks 1-3 |
| REQUIREMENTS | PRVD-04, PRVD-05, XAI-01, XAI-04 | Tasks 1-3 |
| CONTEXT | D-04, D-06, D-09, D-10, D-12 | Keychain/auth in Task 1; request privacy/model in Task 2; probes in Task 3 |
| RESEARCH | Responses `store:false`, fixed xAI origin, official models, device OAuth | Tasks 1-3 |
| PATTERNS | xai_keychain, XaiCredentialManager, XaiAudioClient, xAI router | Extended in place; no SDK/helper duplication |
</source_coverage>

<verification>
Run all focused tests, `npm run typecheck`, and `npm run build`. Inspect built server output and packaged Swift source/binary invocation contract for secret-bearing argv/env strings. Live network/credential acceptance is deferred only to checkpoint 10-05-03; mocks do not mark the phase complete.
</verification>

<success_criteria>
- PRVD-04 credential custody is implemented for direct/gateway slots without building Phase 12 connections.
- PRVD-05 uses real capability request paths, not command presence.
- XAI-01 reuses one credential while readiness remains independent.
- XAI-04 request-level no-storage/no-tool behavior is enforced in code and tests.
</success_criteria>

<output>
Create `.planning/phases/10-provider-model-xai-foundation/10-02-SUMMARY.md` when done.
</output>
