---
phase: 10-provider-model-xai-foundation
plan: 04
type: execute
wave: 3
depends_on: [10-01, 10-02]
files_modified:
  - yulu/scripts/yulu_ui/src/routers/search.ts
  - yulu/scripts/yulu_ui/src/routers/ask.ts
  - yulu/scripts/yulu_ui/src/routers/agentSessions.ts
  - yulu/scripts/yulu_ui/src/agentSessionStore.ts
  - yulu/scripts/yulu_ui/tests/routers/search.test.ts
  - yulu/scripts/yulu_ui/tests/routers/ask.test.ts
  - yulu/scripts/yulu_ui/tests/routers/agentSessions.test.ts
  - yulu/scripts/yulu_ui/tests/agentSessionStore.test.ts
autonomous: true
requirements: [PRVD-02, PRVD-03, PRVD-05, XAI-01, XAI-03, XAI-04]

must_haves:
  truths:
    - "An xAI conversation session is pinned to its creation-time provider/model and keeps all history locally."
    - "Yulu completes local meeting retrieval and applies count/per-excerpt/total/history caps before any xAI request."
    - "Only bounded meeting title/date/excerpt data and bounded local message context are sent; local paths and full files are not sent."
    - "The displayed/persisted sources are the exact normalized local search hits, never model-provided citations."
    - "No matching local evidence causes a local empty-evidence response and zero xAI calls; provider failure pauses without fallback."
  artifacts:
    - path: "yulu/scripts/yulu_ui/src/routers/ask.ts"
      provides: "Pinned Agent/xAI dispatch, local retrieval, bounded prompt, safe source projection"
      contains: "store"
    - path: "yulu/scripts/yulu_ui/src/agentSessionStore.ts"
      provides: "Bounded local context projection and paused session state"
      contains: "paused"
    - path: "yulu/scripts/yulu_ui/src/routers/agentSessions.ts"
      provides: "Server-resolved creation-time conversation identity"
      contains: "create"
  key_links:
    - from: "agentSessions.create"
      to: "config intelligence.conversation + resolved Agent runtime"
      via: "server-side creation snapshot"
      pattern: "conversation"
    - from: "ask.ts local runSearchCli"
      to: "XaiTextClient.answer"
      via: "pure bounded excerpt projection before network call"
      pattern: "runSearchCli"
    - from: "normalized SearchHit[]"
      to: "session message sources / Agent Console"
      via: "local source projection independent of model text"
      pattern: "sources"
---

<objective>
Add cited xAI conversation across local meetings while preserving Yulu's local-first data and history boundaries.

Purpose: Satisfy XAI-03/XAI-04 with the existing search index and session store, and correct the current ask route's use of the latest Agent runtime instead of the session identity.
Output: Server-pinned conversation sessions, bounded local retrieval/history projection, xAI answer dispatch with local sources, and explicit pause behavior.
</objective>

<execution_context>
@/Users/liaoyuxing/.codex/get-shit-done/workflows/execute-plan.md
@/Users/liaoyuxing/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/10-provider-model-xai-foundation/10-CONTEXT.md
@.planning/phases/10-provider-model-xai-foundation/10-RESEARCH.md
@.planning/phases/10-provider-model-xai-foundation/10-PATTERNS.md
@.planning/phases/10-provider-model-xai-foundation/10-VALIDATION.md
@.planning/phases/10-provider-model-xai-foundation/10-01-SUMMARY.md
@.planning/phases/10-provider-model-xai-foundation/10-02-SUMMARY.md

@yulu/scripts/yulu_ui/src/routers/search.ts
@yulu/scripts/yulu_ui/src/routers/ask.ts
@yulu/scripts/yulu_ui/src/routers/agentSessions.ts
@yulu/scripts/yulu_ui/src/agentSessionStore.ts
@yulu/scripts/yulu_ui/web/src/routes/agent-console.tsx

<interfaces>
- `runSearchCli(input, scriptDir)` and `SearchHit` are the local retrieval seam. Preserve the Python index/search implementation; do not add another index.
- `agent-sessions.json` is the local history/source store from Plan 10-01. The server, not the browser, resolves ask-purpose provider/model identity.
- `XaiTextClient.answer` from Plan 10-02 accepts already-bounded question/history/excerpts and enforces `store:false`/no tools.
- Preserve the existing local Agent path's Agent-owned retrieval/connectors. xAI alone uses the Yulu-local bounded retrieval path in this phase.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Resolve conversation identity on the server and enforce it on every turn</name>
  <files>yulu/scripts/yulu_ui/src/routers/agentSessions.ts, yulu/scripts/yulu_ui/src/routers/ask.ts, yulu/scripts/yulu_ui/src/agentSessionStore.ts, yulu/scripts/yulu_ui/tests/routers/agentSessions.test.ts, yulu/scripts/yulu_ui/tests/routers/ask.test.ts, yulu/scripts/yulu_ui/tests/agentSessionStore.test.ts</files>
  <read_first>
    - `routers/agentSessions.ts` create/list/get/append inputs and current client-supplied `agent` field.
    - `routers/ask.ts` current config/runtime resolution and optional session behavior.
    - `agentSessionStore.ts` Plan 10-01 provider/model/status helpers.
  </read_first>
  <behavior>
    - Creating an ask-purpose session resolves `intelligence.conversation` server-side: `xai` + exact configured model, or the actual selected Agent runtime + `runtime-managed`. A browser-supplied Agent label cannot override it.
    - Background Agent Console sessions preserve their current explicit Agent behavior.
    - `ask.ask` requires/loads an active ask session and dispatches from its provider/model. It never reads the current conversation selection to choose an executor.
    - For a pinned Agent session, the current resolved runtime must match `session.provider`; otherwise pause with an actionable mismatch instead of switching.
    - A paused session rejects new turns until explicit same-provider resume/retry or a new session is created. Provider/model never mutates.
  </behavior>
  <action>
    Add route/store tests first for server-side snapshot, malicious client agent input, config/runtime changes after creation, background compatibility, and pause/resume. Update the existing route contracts minimally; keep `agent` as a compatibility projection in responses while making `provider/model/status` authoritative. Update the voice/server caller only as required to create an ask session before calling ask.
  </action>
  <verify>
    <automated>cd yulu/scripts/yulu_ui &amp;&amp; npm test -- --run tests/routers/agentSessions.test.ts tests/agentSessionStore.test.ts tests/routers/ask.test.ts -t 'provider|model|session|paused'</automated>
  </verify>
  <done>Every conversation turn uses a server-created immutable provider/model snapshot, and mismatches pause rather than switch.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Bound local meeting retrieval and history before the xAI trust boundary</name>
  <files>yulu/scripts/yulu_ui/src/routers/search.ts, yulu/scripts/yulu_ui/src/routers/ask.ts, yulu/scripts/yulu_ui/src/agentSessionStore.ts, yulu/scripts/yulu_ui/tests/routers/search.test.ts, yulu/scripts/yulu_ui/tests/routers/ask.test.ts</files>
  <read_first>
    - `routers/search.ts` `SearchHit`, normalization, allowed meeting kinds, and `runSearchCli` fixed argv.
    - Search CLI output examples/tests to understand `[hit]` marker format and source path normalization.
    - Research Threat T-10-04/T-10-06 and Context D-08/D-09.
  </read_first>
  <behavior>
    - Search only `meeting_summary` and `meeting_transcript`, with a hard source count no greater than 8.
    - A pure exported boundary removes `[hit]` markers/control noise, deduplicates by meeting/kind, caps each excerpt (target 1,200 UTF-16 characters), and caps all excerpts (target 6,000); tests include CJK, emoji, oversized, duplicate, and malformed hits.
    - xAI prompt data includes stable reference number, meeting title, recorded date, kind, and excerpt. It excludes `sourcePath`, absolute directories, full file content, search telemetry, and unrelated connector data.
    - Local history projection uses only a bounded tail (target 12 messages and 12,000 characters total), strips source paths/remote connector objects, and preserves roles/text needed for continuity.
    - If no bounded local excerpt remains, return the specified local empty-evidence response and make zero xAI requests.
  </behavior>
  <action>
    Write pure bounding and router call-order tests first. Reuse `runSearchCli`; add only small pure projection functions near the ask route or session store, whichever owns the data. Call search and complete all bounding before invoking `XaiTextClient`. Do not read meeting files, introduce embeddings/vector storage, or send source paths.
  </action>
  <verify>
    <automated>cd yulu/scripts/yulu_ui &amp;&amp; npm test -- --run tests/routers/search.test.ts tests/routers/ask.test.ts -t 'bound|excerpt|history|empty|source'</automated>
  </verify>
  <done>The xAI boundary receives only deterministic bounded local excerpts/history, and empty evidence never causes a remote request.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Return and persist cited local sources while pausing xAI failures</name>
  <files>yulu/scripts/yulu_ui/src/routers/ask.ts, yulu/scripts/yulu_ui/src/agentSessionStore.ts, yulu/scripts/yulu_ui/tests/routers/ask.test.ts, yulu/scripts/yulu_ui/tests/agentSessionStore.test.ts</files>
  <read_first>
    - Existing `ask.ts` response shape and `agentSessionSourceSchema`/message append usage in Agent Console and `server.ts` voice chat.
    - `XaiTextClient.answer` request/response contract and Plan 10-02 request-shape tests.
    - UI-SPEC conversation paused/source behavior.
  </read_first>
  <behavior>
    - xAI answer prompt instructs bracket references to numbered excerpts, but response `sources` always comes from the exact bounded local hits, never parsed model URLs/paths.
    - Successful response returns provider/model, answer, local sources, search timing/count metadata, `usedFallback:false`, and no remote connector context.
    - The caller persists the assistant answer and those local sources in the existing local session. No remote response/session ID is stored or used for continuation.
    - xAI auth/model/network/response failure pauses the session with a safe reason, returns pinned identity and recovery actions, and makes zero Agent/alternate-model calls.
    - Same-provider retry resumes only after explicit user action and rebuilds context from local messages; a changed setting requires a new session.
  </behavior>
  <action>
    Extend current ask/session tests first with injected search/xAI clients, malicious model citation text, request failure, changed provider config, and call-count assertions. Keep the existing response shape additive so current Agent Console code remains functional until Plan 10-05 renders new fields. Do not enable xAI citations/tools, Web/X search, connector writes, or server-side response history.
  </action>
  <verify>
    <automated>cd yulu/scripts/yulu_ui &amp;&amp; npm test -- --run tests/routers/ask.test.ts tests/agentSessionStore.test.ts &amp;&amp; npm run typecheck</automated>
  </verify>
  <done>xAI answers carry auditable local meeting sources and local history, while failures remain pinned, paused, and fallback-free.</done>
</task>

</tasks>

<threat_model>
- **T-10-01 (High):** session rebinding — server snapshot and runtime-match gate.
- **T-10-04 (High):** excessive local data egress — local-first call order plus count/per-item/total/history bounds and no path fields.
- **T-10-05 (High):** remote storage/tools — XaiTextClient request contract; no remote response IDs stored.
- **T-10-06 (Medium):** invented citations — sources derive only from normalized local hits.
- **T-10-07 (High):** fallback after failure — durable pause and zero alternate call assertions.
</threat_model>

<source_coverage>
| Source | Items | Coverage |
|--------|-------|----------|
| GOAL/ROADMAP | Ask across local meetings with local history/sources and bounded xAI data | Tasks 1-3 |
| REQUIREMENTS | PRVD-02, PRVD-03, PRVD-05, XAI-01, XAI-03, XAI-04 | Tasks 1-3 |
| CONTEXT | D-02, D-03, D-08, D-09, D-10 | Server snapshot, bounds, local sources/history, pause |
| RESEARCH | Local search first; no tools/state/files; model citations untrusted | Tasks 2-3 |
| PATTERNS | runSearchCli, ask router, agentSessionStore/source schema | Reused directly; no vector DB/remote history |
</source_coverage>

<verification>
Run all focused route/store tests, `npm run typecheck`, and `npm run build`. Inspect captured xAI request fixtures to confirm no absolute path, full source file, token, remote connector output, tools, or previous response identifier appears. Verify legacy Agent ask behavior remains green.
</verification>

<success_criteria>
- XAI-03 performs bounded local retrieval before xAI and displays exact local source meetings.
- XAI-04 keeps history local and enables no Web/X search, server state, or writes.
- PRVD-02/03 hold across conversation creation, turns, config changes, runtime mismatch, failure, and retry.
- Existing Agent conversation remains available and Hermes is not required for xAI conversation.
</success_criteria>

<output>
Create `.planning/phases/10-provider-model-xai-foundation/10-04-SUMMARY.md` when done.
</output>
