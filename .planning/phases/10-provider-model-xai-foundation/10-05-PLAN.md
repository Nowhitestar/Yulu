---
phase: 10-provider-model-xai-foundation
plan: 05
type: execute
wave: 4
depends_on: [10-03, 10-04]
files_modified:
  - yulu/scripts/yulu_ui/web/src/components/settings/categories.ts
  - yulu/scripts/yulu_ui/web/src/routes/settings.$category.tsx
  - yulu/scripts/yulu_ui/web/src/components/settings/ProviderSection.tsx
  - yulu/scripts/yulu_ui/web/src/components/settings/TranscriptionSection.tsx
  - yulu/scripts/yulu_ui/web/src/routes/agent-console.tsx
  - yulu/scripts/yulu_ui/web/src/routes/inbox/recordings.$stem.tsx
  - yulu/scripts/yulu_ui/web/src/routes/settings.css
  - yulu/scripts/yulu_ui/web/src/i18n/messages.ts
  - yulu/scripts/yulu_ui/tests/web/ProviderSection.test.tsx
  - yulu/scripts/yulu_ui/tests/web/TranscriptionSection.test.tsx
  - yulu/scripts/yulu_ui/tests/web/routes/settings.test.tsx
  - yulu/scripts/yulu_ui/tests/web/routes/agent-console.test.tsx
  - yulu/scripts/yulu_ui/tests/web/recordings.reader.test.tsx
autonomous: false
requirements: [PRVD-01, PRVD-03, PRVD-05, XAI-01, XAI-02, XAI-03, XAI-04]

must_haves:
  truths:
    - "A user sees and changes transcription, summary, and conversation independently from one reachable AI Providers settings category."
    - "One shared xAI credential card never displays a token/key and shows separate real readiness for all three capabilities."
    - "Paused summary/conversation UI names the pinned provider/model and offers same-provider retry, settings, or no action; it never implies fallback."
    - "xAI conversation displays exact local meeting source cards and makes local privacy boundaries visible."
    - "One Yulu-managed Grok CLI-compatible xAI connection passes real STT/summary/conversation probes and the production summary/conversation acceptance flow."
  artifacts:
    - path: "yulu/scripts/yulu_ui/web/src/components/settings/ProviderSection.tsx"
      provides: "Independent capability selectors, shared xAI connection, readiness actions"
      contains: "Test conversation"
    - path: "yulu/scripts/yulu_ui/web/src/routes/agent-console.tsx"
      provides: "Pinned provider/model, local sources, paused conversation actions"
      contains: "sources"
    - path: "yulu/scripts/yulu_ui/web/src/routes/inbox/recordings.$stem.tsx"
      provides: "Provider-paused summary status and retry/settings controls"
      contains: "awaiting_provider"
  key_links:
    - from: "ProviderSection selectors"
      to: "registered config paths"
      via: "existing config.update/restart tracker; each control writes exactly one capability"
      pattern: "intelligence"
    - from: "provider readiness actions"
      to: "providers router"
      via: "separate mutations and status rows"
      pattern: "transcription|summary|conversation"
    - from: "Agent Console source cards"
      to: "ask response/session local sources"
      via: "existing source schema; no model citation parsing"
      pattern: "sources"
---

<objective>
Make the provider/xAI foundation understandable and prove it against the real service and production artifacts.

Purpose: Complete the user-observable Phase 10 contract after the backend seams exist, using Yulu's current design system and one traceable live acceptance checkpoint.
Output: AI Providers settings, explicit paused/source UI, focused accessibility tests, and recorded live xAI evidence.

The 13-file surface follows three existing user surfaces (settings, Agent Console, recording detail) and their colocated tests/i18n/CSS. The plan adds no alternate page, design system, or backend responsibility.
</objective>

<execution_context>
@/Users/liaoyuxing/.codex/get-shit-done/workflows/execute-plan.md
@/Users/liaoyuxing/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/10-provider-model-xai-foundation/10-CONTEXT.md
@.planning/phases/10-provider-model-xai-foundation/10-RESEARCH.md
@.planning/phases/10-provider-model-xai-foundation/10-PATTERNS.md
@.planning/phases/10-provider-model-xai-foundation/10-UI-SPEC.md
@.planning/phases/10-provider-model-xai-foundation/10-VALIDATION.md
@.planning/phases/10-provider-model-xai-foundation/10-03-SUMMARY.md
@.planning/phases/10-provider-model-xai-foundation/10-04-SUMMARY.md

@yulu/scripts/yulu_ui/web/src/components/settings/TranscriptionSection.tsx
@yulu/scripts/yulu_ui/web/src/components/settings/categories.ts
@yulu/scripts/yulu_ui/web/src/routes/settings.$category.tsx
@yulu/scripts/yulu_ui/web/src/routes/agent-console.tsx
@yulu/scripts/yulu_ui/web/src/routes/inbox/recordings.$stem.tsx

<interfaces>
- Follow `10-UI-SPEC.md` exactly. Reuse current settings cards/classes, theme tokens, native controls, lucide-react, i18n, tRPC hooks, and existing message/source components.
- The providers router never returns secret values. UI may submit an API key once, then must clear local input and display status/source only.
- Backend snapshots are authoritative. UI cannot mutate provider/model on an existing task/session; settings changes create future identity only.
- No first-run activation wizard, Agent/gateway connection wizard, calendar, sharing setup, or README work belongs in this plan.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add the AI Providers settings category and independent readiness controls</name>
  <files>yulu/scripts/yulu_ui/web/src/components/settings/categories.ts, yulu/scripts/yulu_ui/web/src/routes/settings.$category.tsx, yulu/scripts/yulu_ui/web/src/components/settings/ProviderSection.tsx, yulu/scripts/yulu_ui/web/src/components/settings/TranscriptionSection.tsx, yulu/scripts/yulu_ui/web/src/routes/settings.css, yulu/scripts/yulu_ui/web/src/i18n/messages.ts, yulu/scripts/yulu_ui/tests/web/ProviderSection.test.tsx, yulu/scripts/yulu_ui/tests/web/TranscriptionSection.test.tsx, yulu/scripts/yulu_ui/tests/web/routes/settings.test.tsx</files>
  <read_first>
    - `10-UI-SPEC.md` Information Architecture, State Matrix, copy, accessibility, and color rules.
    - `TranscriptionSection.tsx` current xAI auth/status/probe card and local provider radio behavior; move/reuse, do not duplicate.
    - `categories.ts`, `settings.$category.tsx`, and consolidated settings test mocking pattern.
  </read_first>
  <behavior>
    - `/settings/llm` is visible as AI Providers/智能服务 after Transcription and before Voice Input, with localized one-line description.
    - Capability rows show Transcription, Summary, Conversation independently. Changing one control sends one exact config update and leaves the other two controls unchanged.
    - Transcription links to detailed local/audio controls. Summary/conversation offer Agent or xAI; xAI model field is visible only for xAI and Agent states `runtime-managed` honestly.
    - One xAI card handles authorize/cancel/logout, explicit API-key fallback set/remove, and status-only credential source. API key is password input, never prefilled/read back, cleared on success.
    - Three readiness rows/buttons run separate probes and show untested/testing/ready/failed, exact tested model/time, and accessible status/error roles. One success cannot turn another row green.
    - `TranscriptionSection` replaces the full duplicate xAI auth card with a compact shared status and link to AI Providers while retaining engine/local model/glossary controls.
  </behavior>
  <action>
    Write focused component and consolidated settings-route tests first. Add the existing `llm` category to visible metadata and one small `ProviderSection`; prefer existing CSS classes and add only selectors needed for the three-row responsive layout. Move/reuse xAI interaction logic rather than keeping two auth forms. Add all locked English/Chinese copy to the existing dictionary. Do not add a new dependency, modal, or wizard.
  </action>
  <verify>
    <automated>cd yulu/scripts/yulu_ui &amp;&amp; npm test -- --run tests/web/ProviderSection.test.tsx tests/web/TranscriptionSection.test.tsx tests/web/routes/settings.test.tsx &amp;&amp; npm run typecheck</automated>
  </verify>
  <done>All capability choices and xAI proofs are reachable, independent, accessible, and backed by one non-leaking connection card.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Render pinned provider failure choices and local meeting sources</name>
  <files>yulu/scripts/yulu_ui/web/src/routes/agent-console.tsx, yulu/scripts/yulu_ui/web/src/routes/inbox/recordings.$stem.tsx, yulu/scripts/yulu_ui/web/src/i18n/messages.ts, yulu/scripts/yulu_ui/web/src/routes/settings.css, yulu/scripts/yulu_ui/tests/web/routes/agent-console.test.tsx, yulu/scripts/yulu_ui/tests/web/recordings.reader.test.tsx</files>
  <read_first>
    - Agent Console session create/send/append/error/source rendering around the existing ask mutation.
    - Recording reader `AgentTaskStatus`, active state set, retry/action buttons, and manual summary error handling.
    - UI-SPEC paused-state copy and source-card contract.
  </read_first>
  <behavior>
    - The Ask Meetings session list filters by `purpose: ask`, not by the currently detected Agent ID, so pinned xAI and prior Agent conversations remain reachable together. New ask sessions let the server resolve the current Conversation Provider.
    - Ask session header/message area shows pinned provider and model. A config change note says it applies to a new conversation.
    - xAI answers render the exact local source meeting title/date/snippet cards already supplied by the backend; paths are used only for local navigation and never rendered as remote citations.
    - Provider failure keeps prior messages/sources visible, marks the session paused, disables ordinary send, and exposes `Retry same provider`, `Open AI Providers`, and `New conversation` actions. No action fires automatically.
    - Recording task status recognizes `awaiting_provider`, shows pinned summary provider/model and safe reason, and exposes same-provider retry plus settings link. Keeping paused is passive and sends nothing.
    - Manual summary failure uses the same explicit copy/actions and never claims the app switched providers.
    - All status/action states are keyboard accessible, non-color-only, localized, stable during loading, and wrap on narrow layouts.
  </behavior>
  <action>
    Add React tests first for successful xAI sources, malicious model citation text (only backend sources render), paused session, paused summary task, action call counts, settings navigation, and independent new-session creation. Extend existing components in place; reuse source/message/task status structures and route navigation. Add no toast system or alternate conversation page.
  </action>
  <verify>
    <automated>cd yulu/scripts/yulu_ui &amp;&amp; npm test -- --run tests/web/routes/agent-console.test.tsx tests/web/recordings.reader.test.tsx &amp;&amp; npm run typecheck</automated>
  </verify>
  <done>Users can see exactly which provider/model is pinned, recover explicitly from failure, and inspect the local meetings behind xAI answers.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Validate one Grok CLI-compatible xAI connection through all three production capabilities</name>
  <files>none (evidence recorded in 10-05-SUMMARY.md and 10-VALIDATION.md)</files>
  <read_first>
    - `10-VALIDATION.md` Blocking Live Checkpoint 10-05-03.
    - Plan 10-02 summary for exact official Grok CLI client-ID equality, the exact reviewed six-scope Yulu subset, and provider probe routes.
    - Plan 10-03/10-04 summaries for exact test recording/session/provider/model identities.
  </read_first>
  <action>
    First run every autonomous Phase 10 verification command, `npm run typecheck`, `npm run build`, the full UI Vitest suite, and the relevant root pytest/Keychain package gates. Do not open this checkpoint on red automation.

    On the installed candidate, reuse the already-authorized Yulu-managed Grok OAuth connection when present; do not ask for redundant authorization. Verify that the packaged client ID exactly equals the official Grok Build client ID and that Yulu requests its reviewed six-scope subset (not Grok Build's additional conversation/workspace scopes), then let each real probe establish the user's actual entitlement. If authorization or a capability entitlement fails, record that factual failure and stop; do not substitute an API key, provider, or model.

    Execute and record:

    1. Real transcription, summary, and conversation probes, each with provider/model/time/result and independently visible status.
    2. One test recording through the production summary path; verify committed transcript, `.summary.md`, HostStore artifact hashes/provenance, exact pinned xAI model, transcript-only request, `store:false`, and no tools.
    3. One question whose evidence spans at least two indexed local meetings; verify local source cards and inspect the captured request for bounded excerpts/history only, no absolute path/full file/connector output/tools/previous response/files.
    4. One invalid pinned model failure; verify durable pause and that no second provider/model call occurs before explicit action.
    5. Credential custody audit across config, SQLite, app logs, process argv, and browser/tRPC responses; token/key must be absent.

    Record exact candidate commit/build identity and redact all credential values from evidence.
  </action>
  <verify>
    <human>Confirm the five evidence groups match `10-VALIDATION.md`, the UI copy/states match `10-UI-SPEC.md`, and there is no silent provider/model/privacy/cost fallback.</human>
  </verify>
  <resume-signal>Approve only after all five groups are green. Grok client/scope drift, a failed required entitlement probe, leaked credential, unbounded request, absent source cards, or automatic fallback blocks Phase 10 completion.</resume-signal>
  <done>One traceable installed candidate proves shared xAI connection, independent readiness, production Markdown summary, bounded cited local conversation, explicit failure pause, and Keychain-only credential custody.</done>
</task>

</tasks>

<threat_model>
- **T-10-02 (High):** browser/response secret exposure — one-way password submission, clear-after-save, status-only render tests.
- **T-10-04/T-10-05 (High):** privacy boundary hidden or violated — UI states local excerpts/`store:false`; live request inspection proves it.
- **T-10-06 (Medium):** invented citations — render only backend local sources, including adversarial model-text test.
- **T-10-07 (High):** implicit fallback through UI actions — no action auto-fires; same-provider retry/new-session distinction is explicit.
- **T-10-09 (High):** Grok OAuth contract/entitlement drift — checkpoint verifies exact official client-ID equality, Yulu's exact reviewed six-scope subset, and all three real capabilities independently.
</threat_model>

<source_coverage>
| Source | Items | Coverage |
|--------|-------|----------|
| GOAL/ROADMAP | Independent selection/readiness, explicit failure, real xAI summary/conversation | Tasks 1-3 |
| REQUIREMENTS | PRVD-01, PRVD-03, PRVD-05, XAI-01..04 | UI in Tasks 1-2; real acceptance in Task 3 |
| CONTEXT | D-01, D-03, D-04, D-06, D-08, D-11, D-12 | Settings/failure/source UI and live Grok compatibility/entitlement gate |
| UI-SPEC | IA, state matrix, copy, tokens, accessibility, registry safety | Tasks 1-2; checker dimensions repeated in checkpoint |
| VALIDATION | 10-05-01..03 and five live evidence groups | Tasks 1-3 |
| PATTERNS | Existing settings cards/category list, Agent Console sources, recording status | Extended in place; no new UI dependency/wizard |
</source_coverage>

<verification>
Automated: focused React tests, full Vitest, `npm run typecheck`, `npm run build`, relevant root pytest/Keychain package gates. Manual/live: checkpoint 10-05-03 exactly; mocks or green status labels alone are insufficient.
</verification>

<success_criteria>
- PRVD-01/05 and XAI-01 are observable through independent selectors and real probes.
- PRVD-03 is observable through pinned failure actions with zero automatic calls.
- XAI-02 is accepted through a real committed Markdown artifact.
- XAI-03/04 are accepted through a real bounded local-meeting answer with local sources and local history.
- UI-SPEC passes all six checker dimensions and no new dependency/design system is added.
</success_criteria>

<output>
Create `.planning/phases/10-provider-model-xai-foundation/10-05-SUMMARY.md` when done and update `10-VALIDATION.md` only with captured green/red evidence.
</output>
