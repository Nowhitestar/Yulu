---
phase: 10-provider-model-xai-foundation
plan: 03
type: execute
wave: 3
depends_on: [10-01, 10-02]
files_modified:
  - yulu/scripts/yulu_ui/src/artifactStore.ts
  - yulu/scripts/yulu_ui/src/recordingPipeline.ts
  - yulu/scripts/yulu_ui/src/routers/recordings.ts
  - yulu/scripts/yulu_ui/src/server.ts
  - yulu/scripts/yulu_ui/tests/artifactStore.test.ts
  - yulu/scripts/yulu_ui/tests/recordingPipeline.test.ts
  - yulu/scripts/yulu_ui/tests/routers/recordings.test.ts
autonomous: true
requirements: [PRVD-02, PRVD-03, PRVD-05, XAI-01, XAI-02]

must_haves:
  truths:
    - "Automatic and manual summaries dispatch through the provider/model selected when that work starts; no later config read can change the request."
    - "xAI receives summary instructions and transcript text only, with no search, connector, audio, full meeting file, or previous conversation context."
    - "Automatic xAI output is staged, size/path/hash validated, and committed as the same `.summary.md` artifact used by the existing Agent path."
    - "A provider failure preserves the transcript, sends no fallback request, and leaves automatic work durably paused until explicit same-provider retry."
  artifacts:
    - path: "yulu/scripts/yulu_ui/src/recordingPipeline.ts"
      provides: "Pinned Agent/xAI summary dispatch and pause behavior"
      contains: "summaryProvider"
    - path: "yulu/scripts/yulu_ui/src/routers/recordings.ts"
      provides: "Manual selected-provider summary regeneration"
      contains: "summarize"
    - path: "yulu/scripts/yulu_ui/src/artifactStore.ts"
      provides: "Provider-neutral staged summary validation"
      contains: "writeStagedSummary"
  key_links:
    - from: "recordingPipeline task.summaryProvider/task.summaryModel"
      to: "XaiTextClient.summarize or pinned Agent gateway"
      via: "one explicit branch after transcript commit"
      pattern: "summaryProvider"
    - from: "XaiTextClient summary output"
      to: "ArtifactStore.commitFromWorkspace + HostStore.recordArtifacts"
      via: "existing production artifact transaction"
      pattern: "commitFromWorkspace"
---

<objective>
Make xAI a real summary provider without weakening Yulu's existing artifact and failure boundaries.

Purpose: Prove XAI-02 end to end and make the automatic summary task honor immutable provider identity instead of assuming Hermes/whatever Agent is current.
Output: Pinned Agent/xAI summary dispatch for automatic and manual paths, real Markdown artifact validation, and explicit provider pause/retry.
</objective>

<execution_context>
@/Users/liaoyuxing/.codex/get-shit-done/workflows/execute-plan.md
@/Users/liaoyuxing/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/10-provider-model-xai-foundation/10-CONTEXT.md
@.planning/phases/10-provider-model-xai-foundation/10-RESEARCH.md
@.planning/phases/10-provider-model-xai-foundation/10-PATTERNS.md
@.planning/phases/10-provider-model-xai-foundation/10-VALIDATION.md
@.planning/phases/10-provider-model-xai-foundation/10-01-SUMMARY.md
@.planning/phases/10-provider-model-xai-foundation/10-02-SUMMARY.md

@yulu/scripts/yulu_ui/src/recordingPipeline.ts
@yulu/scripts/yulu_ui/src/artifactStore.ts
@yulu/scripts/yulu_ui/src/hostStore.ts
@yulu/scripts/yulu_ui/src/agentGateway.ts
@yulu/scripts/yulu_ui/src/routers/recordings.ts
@yulu/scripts/yulu_ui/src/agentActions.ts

<interfaces>
- `ArtifactStore.writeStagedSummary` and `commitFromWorkspace` are the provider-neutral production artifact boundary; keep maximum size, path confinement, atomic writes, SHA-256 records, and transcript+summary paired commit.
- `HostStore` from Plan 10-01 owns immutable `summaryProvider`, `summaryModel`, and `awaiting_provider`; do not mutate identity in the pipeline.
- `XaiTextClient` from Plan 10-02 owns request privacy/response parsing. This plan supplies transcript/instructions and consumes returned text only.
- Preserve the existing Agent gateway/isolated manual Agent action. Agent runtime connection redesign is Phase 12.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Branch the automatic pipeline on its pinned summary provider</name>
  <files>yulu/scripts/yulu_ui/src/artifactStore.ts, yulu/scripts/yulu_ui/src/recordingPipeline.ts, yulu/scripts/yulu_ui/src/server.ts, yulu/scripts/yulu_ui/tests/artifactStore.test.ts, yulu/scripts/yulu_ui/tests/recordingPipeline.test.ts</files>
  <read_first>
    - `recordingPipeline.ts` prepare/persist, dispatch loop, gateway cache, `runTask`, and `AgentUnavailableError` retry catch.
    - `artifactStore.ts:55-215` staged transcript/summary and commit transaction; change only the provider-specific empty-summary wording.
    - Plan 10-01 HostStore summary fields/claim/pause and Plan 10-02 XaiTextClient interface.
  </read_first>
  <behavior>
    - `prepare()` reads current config once, resolves `agent` to the actual runtime provider or keeps `xai`, and stores exact model (`runtime-managed` for Agent) before enqueue.
    - Dispatch peeks/claims the oldest task without rewriting identity, then constructs only the executor required by its pinned summary provider. Idle/xAI tasks do not resolve an Agent gateway.
    - Agent tasks run only when the resolved runtime identity matches the pinned task provider; mismatch pauses rather than rebinding.
    - xAI tasks commit/reload the transcript as today, call `xaiText.summarize({model, instructions, transcript})`, stage output, commit both transcript+summary through `commitFromWorkspace`, and record both artifacts/provenance.
    - Provenance contains summary provider, exact model, transcription provider/chunks, and `remoteStore:false`, but no prompt/transcript/token. Existing Notion delivery remains a separate pinned Agent action and is not silently used to create the summary.
  </behavior>
  <action>
    Extend pipeline tests first with fake Agent gateway and fake xAI text client: Agent task, xAI task, changed config after enqueue, no Agent resolution for xAI, exact transcript/instructions, committed `.summary.md`, and provenance. Inject the xAI client through `RecordingPipelineOptions` and server wiring. Use one direct branch in `runTask`; do not create a provider registry/factory. Reuse the existing workspace/artifact methods and make their error copy provider-neutral.
  </action>
  <verify>
    <automated>cd yulu/scripts/yulu_ui &amp;&amp; npm test -- --run tests/artifactStore.test.ts tests/recordingPipeline.test.ts</automated>
  </verify>
  <done>Automatic xAI and Agent summaries honor the task snapshot and produce the same verified production artifacts without unnecessary executor construction.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Route manual summary regeneration through the selected provider snapshot</name>
  <files>yulu/scripts/yulu_ui/src/routers/recordings.ts, yulu/scripts/yulu_ui/tests/routers/recordings.test.ts</files>
  <read_first>
    - `routers/recordings.ts:930-990` transcribe/summarize manual action lock, prompt snapshot, Agent call, glossary, atomic write, stale marker, and jobs events.
    - `agentActions.ts:130-170` transcript-only Agent prompt and isolated session behavior.
    - Context D-02, D-07, D-10 — snapshot once, transcript-only xAI, exact model.
  </read_first>
  <behavior>
    - At mutation start, read summary selection once and resolve it to `{provider, model}`; do not re-read config after awaiting network/Agent execution.
    - Existing prompt template rendering and glossary instructions apply to both providers.
    - Agent selection preserves `runAgentSummarize`. xAI selection reads only the selected transcript file, calls `XaiTextClient.summarize`, applies the glossary contract, and uses the existing atomic `.summary.md`/stale-marker flow.
    - Return provider/model/session metadata safe for UI. No xAI credentials, prompt body, or transcript is returned/logged.
    - Failure writes neither summary nor clear-stale marker and never invokes the other provider.
  </behavior>
  <action>
    Add router tests first for Agent compatibility, xAI transcript-only request, config change during an in-flight promise, output atomicity, and zero fallback calls. Add the smallest branch inside the current `summarize` mutation; do not create a second endpoint or duplicate prompt/glossary/file logic.
  </action>
  <verify>
    <automated>cd yulu/scripts/yulu_ui &amp;&amp; npm test -- --run tests/routers/recordings.test.ts -t summary</automated>
  </verify>
  <done>Manual regeneration supports xAI and Agent with one creation-time selection and unchanged artifact/stale semantics.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Replace automatic retry/fallback with explicit provider pause and same-provider retry</name>
  <files>yulu/scripts/yulu_ui/src/recordingPipeline.ts, yulu/scripts/yulu_ui/src/routers/recordings.ts, yulu/scripts/yulu_ui/tests/recordingPipeline.test.ts, yulu/scripts/yulu_ui/tests/routers/recordings.test.ts</files>
  <read_first>
    - `recordingPipeline.ts` retry delay/constants, catch branch, `retry(id)`, and task publish events.
    - Plan 10-01 `HostStore.pauseForProvider`/`retry` contract.
    - UI-SPEC summary failure actions; backend provides retry-same only, settings navigation is frontend-only.
  </read_first>
  <behavior>
    - Summary auth/model/request/unavailability errors after transcript commit call the provider-pause method once, release the lease, publish a safe paused/failure event, and stop dispatch.
    - No exponential automatic retry remains for provider failures; transient same-request network retry inside XaiTextClient does not change provider/model.
    - `retry(id)` requeues the same snapshot only. It never reads current summary settings or changes provider/model.
    - The recordings API exposes pinned provider/model, paused reason, and explicit same-provider retry. Existing cancel/delete/recovery behavior remains consistent.
    - Manual failure remains a single explicit attempt and returns an actionable error; UI Plan 10-05 offers retry/open-settings/keep-paused without automatic calls.
  </behavior>
  <action>
    Write call-count/state tests first: invalid xAI model, unavailable pinned Agent, config changed to a working alternative, and same-provider retry. Assert one failed provider call, zero alternative calls, transcript/artifact preservation, paused persistence across a new HostStore, and immutable identity. Remove only provider-level automatic retry scheduling; keep unrelated dispatcher polling/recovery behavior.
  </action>
  <verify>
    <automated>cd yulu/scripts/yulu_ui &amp;&amp; npm test -- --run tests/recordingPipeline.test.ts tests/routers/recordings.test.ts -t 'provider|summary|retry|paused'</automated>
  </verify>
  <done>Provider failures are explicit durable pauses with same-provider retry, never hidden switching or repeated spend.</done>
</task>

</tasks>

<threat_model>
- **T-10-01 (High):** task rebinding — task snapshot drives the sole branch; runtime mismatch pauses.
- **T-10-04/T-10-05 (High):** unintended data/tool egress — summary inputs are instructions+transcript only; xAI client enforces no tools/state and `store:false`.
- **T-10-07 (High):** fallback/retry spend — automatic provider retry removed; tests assert one provider call and durable pause.
- **T-10-08 (Medium):** corrupt model artifact — existing size/path/hash/atomic artifact transaction is mandatory for xAI.
</threat_model>

<source_coverage>
| Source | Items | Coverage |
|--------|-------|----------|
| GOAL/ROADMAP | Pinned summary, no fallback, saved xAI Markdown | Tasks 1-3 |
| REQUIREMENTS | PRVD-02, PRVD-03, PRVD-05, XAI-01, XAI-02 | Tasks 1-3 |
| CONTEXT | D-02, D-03, D-06, D-07, D-10 | Automatic/manual dispatch and failure controls |
| RESEARCH | Existing artifact transaction; branch at execution seam; failure as pause | Tasks 1 and 3 |
| PATTERNS | ArtifactStore, RecordingPipeline, manual recordings mutation | Reused directly; no second summary endpoint/store |
</source_coverage>

<verification>
Run the three focused commands, then `npm run typecheck &amp;&amp; npm run build`. Source-audit both summary call sites for current-config reads after creation and for any xAI access to search/connectors/audio. Confirm xAI automatic artifacts are readable through `ArtifactStore.readCommittedSummary` and indexed by the existing recording flow.
</verification>

<success_criteria>
- XAI-02 produces saved Markdown through the production artifact path from transcript-only content.
- PRVD-02 holds across enqueue, dispatch, manual start, config changes, and retry.
- PRVD-03 holds with no automatic provider/model fallback.
- Existing Agent summary and optional delivery behavior remains compatible.
</success_criteria>

<output>
Create `.planning/phases/10-provider-model-xai-foundation/10-03-SUMMARY.md` when done.
</output>
