---
phase: 05-capability-reuse-data-folder-cloud-sync-safety
plan: 04
type: execute
wave: 2
depends_on: [05-01, 05-03]
files_modified:
  - yulu/scripts/yulu_ui/src/config.ts
  - yulu/scripts/yulu_ui/web/src/components/InlineEditRow.tsx
  - yulu/scripts/yulu_ui/web/src/components/settings/StorageSection.tsx
  - yulu/scripts/yulu_ui/tests/web/routes/settings.test.tsx
  - yulu/scripts/yulu_ui/tests/web/InlineEditRow.cloudwarn.test.tsx
autonomous: false
requirements: [DATA-01, DATA-03]
must_haves:
  truths:
    - "Changing audio.output_dir reports the audio daemon as needing restart (no longer 'none')"
    - "Choosing a cloud-sync folder shows an eviction/corruption warning BEFORE the value is committed"
    - "The user can opt in to a cloud folder anyway (detect-and-warn, NOT block)"
    - "A detection failure never blocks folder selection (degrades to no-warn)"
  artifacts:
    - path: "yulu/scripts/yulu_ui/src/config.ts"
      provides: "audio.output_dir mapped to restart:audiodaemon (DATA-01 propagation)"
      contains: "restart:audiodaemon"
    - path: "yulu/scripts/yulu_ui/web/src/components/InlineEditRow.tsx"
      provides: "cloud-warn interception in the folder PathValue choose() flow"
      contains: "cloud"
  key_links:
    - from: "yulu/scripts/yulu_ui/web/src/components/InlineEditRow.tsx"
      to: "system cloud detect tRPC route"
      via: "PathValue.choose() detects then warns before onCommit"
      pattern: "cloud|detect"
    - from: "yulu/scripts/yulu_ui/src/config.ts"
      to: "daemonsNeedingRestart for audio.output_dir"
      via: "RESTART_MAP entry restart:audiodaemon"
      pattern: "output_dir|audiodaemon"
---

<objective>
Wire the cloud-capable folder picker: (1) fix DATA-01 propagation so changing the data-folder actually restarts the audio daemon (today `audio.output_dir` maps to `"none"`, but the daemon caches RECORDING_DIR at start and needs a restart); (2) surface the DATA-03 cloud-root WARNING in the folder-picker flow — detect the chosen folder via Plan 03's `cloud.detect`, warn about eviction/corruption risk, and let the user opt in (detect-and-warn, NOT block). LAST piece (Wave 2): depends on Plan 01's runtime/content split + lock (D-06: the picker ships only AFTER DATA-02, so runtime can never be routed into the synced folder) and Plan 03's detection primitive.

Purpose: A user can configure the data-folder and the change takes effect across daemons (D-02/DATA-01); when that folder is a detected cloud root, they see the honest risk before accepting (D-03/DATA-03). Only CONTENT moves — runtime/state stays locked machine-local (enforced by Plan 01).
Output: `audio.output_dir` -> `restart:audiodaemon`; a cloud-warn interception in the folder picker; updated settings test mocks; a focused cloud-warn test; a human-verify checkpoint for the live-cloud + live-restart smokes.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-capability-reuse-data-folder-cloud-sync-safety/05-CONTEXT.md
@.planning/phases/05-capability-reuse-data-folder-cloud-sync-safety/05-RESEARCH.md
@.planning/phases/05-capability-reuse-data-folder-cloud-sync-safety/05-VALIDATION.md
@.planning/phases/05-capability-reuse-data-folder-cloud-sync-safety/05-03-PLAN.md

@yulu/scripts/yulu_ui/src/config.ts
@yulu/scripts/yulu_ui/web/src/components/InlineEditRow.tsx
@yulu/scripts/yulu_ui/web/src/components/settings/StorageSection.tsx
@yulu/scripts/yulu_ui/web/src/components/settings/AudioSection.tsx
@yulu/scripts/yulu_ui/tests/web/routes/settings.test.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix DATA-01 propagation — audio.output_dir -> restart:audiodaemon (RESEARCH Pitfall 5)</name>
  <read_first>
    - `yulu_ui/src/config.ts:55-76` RESTART_MAP — `audio.output_dir` is `"none"` (line 61), so a data-folder change triggers NO daemon restart. `classify()` (131-148) reads this map; `update()` (104-115) returns `daemonsNeedingRestart`; sections call `tracker.record(vars.key, res.daemonsNeedingRestart)`.
    - RESEARCH Pitfall 5 + Open-Q3: NO plist injects `YULU_OUTPUT_DIR` (verified across all 8 plists); the audio daemon reads `audio.output_dir` from config.json and CACHES `RECORDING_DIR = loadRecordingDir()` at process start (audio_daemon.swift:60). A new default needs an AUDIO DAEMON RESTART — not a plist re-render, not SIGHUP. Per-recording overrides flow via the socket start request (line 1117), unaffected.
    - RESEARCH Data-Folder Propagation: propagation = config.json write (already atomic in config.ts) + daemon reload. Only the RESTART_MAP tag is wrong.
    - Surgical change: `audio.output_dir` -> `"restart:audiodaemon"`. Do NOT add a plist env var; do NOT add SIGHUP (the daemon caches at start; restart is correct). The longest-prefix classify is unaffected (output_dir has no sub-keys in the map).
  </read_first>
  <files>yulu/scripts/yulu_ui/src/config.ts</files>
  <action>
    In config.ts RESTART_MAP, change `audio.output_dir` from `"none"` to `"restart:audiodaemon"` (DATA-01: the data-folder change must take effect across daemons; the audio daemon caches RECORDING_DIR and needs a restart — RESEARCH Pitfall 5). Update the adjacent comment to note WHY (daemon caches the recording dir at start; no plist env var carries output_dir). Do not touch plists or add SIGHUP. Leave every other RESTART_MAP entry intact.
  </action>
  <verify>
    <automated>cd yulu/scripts/yulu_ui; npx tsc --noEmit -p tsconfig.json; grep -q 'restart:audiodaemon' src/config.ts</automated>
  </verify>
  <done>RESTART_MAP maps audio.output_dir to restart:audiodaemon; a config.update on audio.output_dir returns the audio daemon in daemonsNeedingRestart; typecheck passes; no plist changed; no SIGHUP added.</done>
</task>

<task type="auto">
  <name>Task 2: Cloud-warn interception in the folder picker (DATA-03) + settings test mocks</name>
  <read_first>
    - `web/src/components/InlineEditRow.tsx:138-152` `PathValue({value, mode, filter, onCommit})` — the folder picker. `choose()` (141-144) calls `trpc.system.pickFile.mutateAsync({mode, filter})` then immediately `onCommit(res.path)`. EXACT interception point: after pickFile returns a path, for `mode === "folder"`, call Plan 03's `cloud.detect`; if `is_cloud`, show the warning + opt-in confirm; `onCommit` only on confirm (or immediately when not cloud).
    - Plan 03 (05-03-PLAN.md dependency) added the `cloud.detect(path)` tRPC route returning `{is_cloud, engine, reason, dataless}`, degrading to a typed not-cloud default on failure. Detection failure must NOT block selection — fall through to onCommit (no warn).
    - RESEARCH "Code Examples: Detect a cloud root at folder-pick time" — honest warning copy: name the engine (res.reason); risks (macOS may EVICT a recording not used recently -> lost/corrupted mid-write or before transcription; Yulu keeps its DBs OUT of this folder so only recordings/transcripts/summaries sync); "You can use this folder anyway if you understand the trade-off." DO NOT block (D-03).
    - RESEARCH Anti-Patterns + Pitfall 3: the copy must cite EVICTION + DB-corruption-if-runtime-leaked, NEVER "a socket cannot exist here" (false). Frame around real harms.
    - Gate the whole cloud branch on `mode === "folder"` so file pickers (model selection etc.) are unaffected.
    - `tests/web/routes/settings.test.tsx:7` mocks trpc; line 46 mocks `pickFile`; 59/62 mock host_capabilities/detected_models. STATE.md [04-02]/[04-03] recurring trap: a new trpc query/mutation a rendered section calls CRASHES the consolidated render until its mock is added. Add a `system.cloud.detect` (or systemRouter cloud detect) mock (default not-cloud) so StorageSection/AudioSection stay green.
    - Confirmation UX: avoid window.confirm (brittle to test). Prefer in-component state rendering an inline warning block with Accept/Cancel, so the cloud-warn test can assert the warning text appears and onCommit fires only after Accept. Match the existing inline-edit aesthetic; no new heavy dependency (React state only).
  </read_first>
  <files>yulu/scripts/yulu_ui/web/src/components/InlineEditRow.tsx, yulu/scripts/yulu_ui/web/src/components/settings/StorageSection.tsx, yulu/scripts/yulu_ui/tests/web/routes/settings.test.tsx</files>
  <action>
    In `InlineEditRow.tsx` PathValue, add a cloud detect hook and rework `choose()`: after pickFile returns a path, if `mode === "folder"`, call cloud.detect({path}); if `is_cloud`, set component state to show an inline warning block (engine/reason + the eviction/corruption copy from RESEARCH, with Accept and Cancel) and DEFER the commit — `onCommit(path)` runs only on Accept; if not cloud (or detection failed/threw), `onCommit(path)` immediately (no warn, never block). Keep file-mode pickers unchanged (gate the cloud branch on `mode === "folder"`). The copy cites eviction + DB-corruption (Pitfall 3), never socket-impossibility. In settings.test.tsx, add the cloud.detect mock (default not-cloud) so the consolidated render stays green ([04-02]/[04-03] trap). StorageSection stays the canonical data-folder picker; the shared InlineEditRow change covers AudioSection's duplicate output_dir picker too (mock is the only extra). No fenced code in summaries.
  </action>
  <verify>
    <automated>cd yulu/scripts/yulu_ui; npx vitest run tests/web/routes/settings.test.tsx</automated>
  </verify>
  <done>The folder picker detects a chosen cloud folder, shows an inline eviction/corruption warning, and commits only after the user accepts (immediate commit when not cloud / on detection failure); file pickers unchanged; settings.test.tsx green with the new cloud.detect mock.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Focused cloud-warn component test (DATA-03 detect-and-warn-not-block)</name>
  <read_first>
    - `tests/web/CapabilitiesSection.test.tsx:1-30` — the canonical section-test idiom: a mutable `queryReturn` holder the trpc mock reads, `vi.mock("../../web/src/trpc.js", ...)`, render + assert. Mirror it for a focused InlineEditRow folder-picker test.
    - The behaviors to prove (D-03 detect-and-warn, NOT block): (a) picking a cloud folder shows the warning and does NOT immediately commit; (b) Accept commits the path; (c) Cancel does not commit; (d) picking a non-cloud folder commits immediately with no warning; (e) a detection error commits immediately (never blocks). Mock `system.pickFile` to return a fixed path and `cloud.detect` to return cloud / not-cloud / throw per test.
    - File location: `yulu/scripts/yulu_ui/tests/web/` (sibling of CapabilitiesSection.test.tsx) — the web vitest root. Name it `InlineEditRow.cloudwarn.test.tsx`.
    - Assert the warning copy mentions eviction (the honest harm) and that onCommit (a vi.fn passed as the row's onCommit) is called/not-called per the table.
  </read_first>
  <files>yulu/scripts/yulu_ui/tests/web/InlineEditRow.cloudwarn.test.tsx</files>
  <behavior>
    - Picking a folder that cloud.detect flags as is_cloud shows a warning mentioning eviction and does NOT call onCommit yet.
    - Clicking Accept calls onCommit with the chosen path.
    - Clicking Cancel does not call onCommit.
    - Picking a folder cloud.detect reports not-cloud calls onCommit immediately, no warning rendered.
    - cloud.detect throwing / erroring calls onCommit immediately (never blocks selection).
  </behavior>
  <action>
    Create `tests/web/InlineEditRow.cloudwarn.test.tsx`. Mock trpc (system.pickFile.useMutation returning a fixed path; a mutable holder for cloud.detect's result so each test drives cloud / not-cloud / throw). Render an InlineEditRow type="path" mode="folder" with a vi.fn onCommit. Drive choose(), then assert per the behavior table: warning visible + no commit for cloud; commit after Accept; no commit after Cancel; immediate commit for not-cloud; immediate commit on detection error. Assert the warning text includes the eviction wording. Mirror the CapabilitiesSection.test mutable-holder idiom.
  </action>
  <verify>
    <automated>cd yulu/scripts/yulu_ui; npx vitest run tests/web/InlineEditRow.cloudwarn.test.tsx</automated>
  </verify>
  <done>InlineEditRow.cloudwarn.test.tsx green; proves cloud -> warn + deferred commit, Accept -> commit, Cancel -> no commit, not-cloud -> immediate commit, detection error -> immediate commit (detect-and-warn, never block).</done>
</task>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 4: Live-cloud warning + live audio-daemon restart smoke (manual — real iCloud/Drive + running daemons)</name>
  <files>yulu/scripts/yulu_ui/web/src/components/InlineEditRow.tsx, yulu/scripts/yulu_ui/src/config.ts</files>
  <action>
    HUMAN VERIFICATION ONLY - no code is written in this task. After Tasks 1-3 land and the suite is green, run the live stack and perform the six checks in <how-to-verify>, reporting pass/fail. These three live-sync behaviors (cloud warning against a REAL iCloud/Drive folder, the audio-daemon restart picking up a new data-folder, and real eviction reporting SF_DATALESS) physically cannot be unit-tested — CI has no live sync engine, no running daemon stack, and cannot induce eviction. Do NOT write fake automated asserts for these; they are genuine human checks (RESEARCH Validation "What needs REAL iCloud/Drive" + 05-VALIDATION Manual-Only table). This is the phase's data-loss-safety gate.
  </action>
  <what-built>
    The cloud-capable data-folder picker is fully wired: (1) `audio.output_dir` now maps to `restart:audiodaemon` so a data-folder change restarts the audio daemon; (2) the folder picker detects a cloud-sync root and shows an eviction/corruption warning before accepting, with opt-in. Unit tests cover detection (mocked SF_DATALESS + path-prefix) and the warn-not-block flow. The three live behaviors below are the manual gate.
  </what-built>
  <how-to-verify>
    1. Start the stack (yulu_ui running) and open Settings -> Storage.
    2. Click "Choose…" on Output directory and pick a real iCloud folder (`~/Library/Mobile Documents/com~apple~CloudDocs/...`) OR a Google Drive folder (`~/Library/CloudStorage/GoogleDrive-<account>/...`). EXPECT: a warning appears naming the engine and citing eviction/corruption risk, BEFORE the value is committed; you can Accept (opt in) or Cancel.
    3. Pick a normal local folder (e.g. `~/Movies/Yulu2`). EXPECT: NO warning; it commits immediately; the row shows the "restart" (⟳) indicator for the audio daemon.
    4. Apply the data-folder change, restart the audio daemon (via the Daemons page or the `yulu` CLI), record a short clip. EXPECT: the new recording lands in the new folder; the status_agent menu reflects it.
    5. Confirm runtime stays put: `ls ~/.config/yulu/*.sqlite ~/.config/yulu/*.sock` still resolves there; NO *.sqlite/*.sock appears in the chosen data-folder (DATA-02 holds — Plan 01).
    6. (Optional, if you can induce eviction via "Optimise Mac Storage" + disk pressure or `brctl evict`) confirm an evicted file in the cloud folder reports SF_DATALESS — the harm the warning describes is real.
  </how-to-verify>
  <verify>
    <human-check>
      Human confirms on real machines: (2) a cloud folder triggers the eviction/corruption warning before commit and is opt-in (NOT blocked); (3) a local-folder change commits immediately and flags the audio daemon for restart; (4) after restart, new recordings land in the new folder; (5) runtime SQLite/sockets remain in ~/.config/yulu and never appear in the data-folder. No automated substitute exists — CI has no live sync engine, no running daemons, and cannot induce eviction.
    </human-check>
  </verify>
  <resume-signal>Type "approved" if the cloud warning appears (and is opt-in, not a block), the local-folder change restarts the audio daemon and new recordings land correctly, and runtime stays in ~/.config/yulu — or describe what diverged.</resume-signal>
  <done>Human has signed off pass on: cloud-folder warning (opt-in, not block); local-folder change restarts the audio daemon and new recordings land in the new folder; runtime stays machine-local (no SQLite/sockets in the data-folder) — or logged a blocking finding.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| UI folder path -> config.update / cloud.detect | A user-chosen path is persisted to config.json and classified for sync risk |
| audio.output_dir -> audio daemon restart | A config value drives a daemon lifecycle action (restart) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-11 | Tampering / EoP | A crafted output_dir path persisted to config.json | mitigate | config.ts validates the whole config against ConfigSchema before write (output_dir is z.string()); PathResolver.data_dir() expanduser+resolves on read (Plan 01); no path is shell-interpolated. ASVS V5. |
| T-05-12 | Spoofing (UX) / safety bypass | A cloud folder accepted with no warning -> silent data-loss risk | mitigate | The picker calls cloud.detect for every folder selection and shows the warning before commit; a detection FAILURE degrades to commit (no false safety claimed) but the runtime LOCK (Plan 01) still prevents the catastrophic case (runtime in a synced folder). DATA-03 is warn, not block (D-03), by design. |
| T-05-13 | DoS | A slow cloud.detect blocking folder selection | mitigate | The Plan 03 route caps the python subprocess with a SIGKILL timeout and degrades to not-cloud; the picker never hangs (falls through to commit). |
| T-05-14 | Tampering | Moving content while a recording is active (orphaned WAV / race) | accept | RESEARCH Pitfall 6 notes the risk; Phase 5 scope is the picker + warn, and the audio daemon RESTART (Task 1) is the apply mechanism. A recording-active guard is noted as a follow-up (Pitfall 6); the existing recording_lock already arbitrates capture; this plan does not move existing files (Phase 7). |
| T-05-SC | Tampering | npm/pip/cargo installs | mitigate | No new package (existing TS deps + Plan 03's stdlib python route); slopcheck N/A per RESEARCH Package Legitimacy Audit; no [ASSUMED]/[SUS] package -> no blocking-human checkpoint for packages (the blocking checkpoint here is the live-cloud smoke, not a package gate). |
</threat_model>

<verification>
- `npx vitest run tests/web/routes/settings.test.tsx tests/web/InlineEditRow.cloudwarn.test.tsx` green (run from yulu/scripts/yulu_ui).
- `npx tsc --noEmit -p tsconfig.json` passes (from yulu/scripts/yulu_ui).
- `grep -n 'restart:audiodaemon' yulu/scripts/yulu_ui/src/config.ts` shows audio.output_dir remapped.
- `grep -ni 'socket.*cannot\|cannot.*exist' yulu/scripts/yulu_ui/web/src/components/InlineEditRow.tsx` returns nothing (Pitfall 3 — no impossibility copy).
- Human-verify checkpoint approved (live-cloud warning + live audio-daemon restart + runtime stays machine-local).
</verification>

<success_criteria>
- DATA-01: the data-folder is configurable and the change takes effect across daemons (audio.output_dir now restarts the audio daemon — RESEARCH Pitfall 5 fix).
- DATA-03: a detected cloud-sync root triggers an honest eviction/corruption warning before acceptance; the user may opt in (detect-and-warn, NOT block — D-03).
- Warning copy cites eviction + DB-corruption, never socket-impossibility (Pitfall 3).
- D-06 honored: this picker (Wave 2) ships only after Plan 01's runtime/content split + lock (Wave 1) — runtime can never be routed into the synced folder.
- The live-sync confirmations are a human-verify checkpoint, not faked.
</success_criteria>

<output>
Create `.planning/phases/05-capability-reuse-data-folder-cloud-sync-safety/05-04-SUMMARY.md` when done.
</output>
