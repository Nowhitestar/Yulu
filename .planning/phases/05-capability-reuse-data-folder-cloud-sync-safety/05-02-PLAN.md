---
phase: 05-capability-reuse-data-folder-cloud-sync-safety
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - yulu/scripts/capabilities/probes.py
  - yulu/scripts/doctor.py
  - yulu/scripts/lib/common.sh
  - yulu/scripts/setup_deps.sh
  - yulu/scripts/setup_capabilities.sh
  - tests/test_reuse_gating.py
autonomous: true
requirements: [REUSE-01, REUSE-02]
must_haves:
  truths:
    - "A host whisper-cli / mlx-whisper / gog reported status==usable causes its install to be skipped"
    - "present-but-unverified OR absent both install Yulu's own (tri-state never collapsed to a boolean)"
    - "doctor.py --json includes a gog capability so REUSE-01's gog reuse can be gated"
    - "The reuse gate never executes a capability's resolved_path in a shell (fixed argv + JSON-parse-in-Python)"
  artifacts:
    - path: "yulu/scripts/lib/common.sh"
      provides: "capability_status() helper reading the host_capabilities tri-state"
      contains: "capability_status"
    - path: "tests/test_reuse_gating.py"
      provides: "parametrized tri-state to skip/install gate test (REUSE-01/02)"
      min_lines: 30
  key_links:
    - from: "yulu/scripts/setup_deps.sh"
      to: "doctor.py --json whisper_cli tri-state"
      via: "capability_status gates brew install whisper-cpp on usable"
      pattern: "capability_status|usable"
    - from: "yulu/scripts/doctor.py"
      to: "capabilities.probes.probe_command"
      via: "gog probe added to host_capabilities"
      pattern: "gog|probe_command"
---

<objective>
Make Yulu reuse-first: when the Phase 3 tri-state report marks a host `whisper-cli` / `mlx-whisper` / `gog` as `usable`, skip installing Yulu's own (D-04, D-05). Add the missing `gog` capability to the report so REUSE-01's wording (whisper / model / claude / gog) is fully covered. INDEPENDENT of the data-folder work — runs in parallel (Wave 1).

Purpose: No unconditional `brew install whisper-cpp`, no duplicate MLX venv when the host already provides a usable one (REUSE-02); never silently mutate the host's package manager. The tri-state (not a boolean) gates the decision so a broken/unimportable host tool still triggers Yulu's own install.
Output: a `gog` probe in `doctor.py`; a `capability_status()` bash helper in `lib/common.sh`; gated `setup_deps.sh` + `setup_capabilities.sh`; a Wave-0 parametrized gate test.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-capability-reuse-data-folder-cloud-sync-safety/05-CONTEXT.md
@.planning/phases/05-capability-reuse-data-folder-cloud-sync-safety/05-RESEARCH.md

@yulu/scripts/capabilities/probes.py
@yulu/scripts/setup_deps.sh
@yulu/scripts/setup_capabilities.sh
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add the gog capability probe to host_capabilities (REUSE-01, closes Open-Q1)</name>
  <read_first>
    - `capabilities/probes.py:123-138` `probe_command(binary, version_args)` resolves a host CLI via the login-shell PATH, returns USABLE host-path or `report.absent(...)`; never executes free-form input (T-03-02). The 1:1 primitive to reuse for gog.
    - `doctor.py:214-273` `_host_capabilities()`: lines 252-258 add claude/whisper_cli/mlx_whisper/llm_command/models/recording_dir; whole body try/except-degraded (T-03-07); `probe_command` already imported (240-246). Add gog here.
    - RESEARCH Open-Q1 / A2: gog NOT in the current report; REUSE-01 names it; `setup_deps.sh:46` installs `steipete/tap/gogcli` unconditionally. CONFIRMED via `doctor.py --json`: no gog key today.
    - The formula `steipete/tap/gogcli` provides the `gog` command. Probe the command name `gog`. `probe_command` degrades version to "" and still reports USABLE when the binary resolves on the login PATH (so a missing `--version` does not break detection).
  </read_first>
  <files>yulu/scripts/doctor.py, yulu/scripts/capabilities/probes.py</files>
  <action>
    In `doctor.py:_host_capabilities` after the recording_dir line (258), add a `gog` entry built with `probe_command("gog", ("--version",))` (import already present). No probes.py logic change is needed; optionally add a one-line docstring mention that gog is among probe_command's callers. Keep the whole-body try/except intact. Do NOT add gog to any CapabilityProvider — it is a host CLI (host-path provenance), not an agent-config reframe; D-06 provider neutrality stays intact.
  </action>
  <verify>
    <automated>python3 -c "import sys,pathlib; sys.path.insert(0,'yulu/scripts'); import doctor; d=pathlib.Path.home()/'.config/yulu'; r=doctor._host_capabilities(d,d); assert 'gog' in r.get('capabilities',{}); print('gog present')"</automated>
  </verify>
  <done>host_capabilities.capabilities contains a `gog` entry: usable/host-path when gog is on the login PATH, absent otherwise; no provider changed; the section still never raises.</done>
</task>

<task type="auto">
  <name>Task 2: capability_status() in lib/common.sh + gate setup_deps.sh and setup_capabilities.sh (REUSE-02, D-04/D-05)</name>
  <read_first>
    - `lib/common.sh`: helpers ok/warn/err/header (31-44); PYTHON_BIN resolution at line 124; SCRIPT_DIR available to sourcing scripts. The `capability_status()` helper lands here (shared by both setup scripts).
    - RESEARCH Pattern 3: the helper runs `doctor.py --json` (fixed argv), parses `host_capabilities.capabilities.<cap>.status` in Python, echoes the status; on ANY error echoes `absent` (safe default = install). NEVER shell-interpolate a resolved_path (T-03-02). CONFIRMED nesting: host_capabilities.capabilities.<cap>.status.
    - RESEARCH Pitfall 4: gate STRICTLY on status == usable. present-but-unverified AND absent both install Yulu's own. Forbidden: `-n "$status"` or `"$status" != "absent"` (collapses the tri-state; report.py:35).
    - `setup_deps.sh:43` installs sox ffmpeg whisper-cpp terminal-notifier; `:46` installs steipete/tap/gogcli. Gate whisper-cpp (REUSE-02) and gogcli (REUSE-01) individually; sox/ffmpeg/terminal-notifier/cloudflared stay unconditional.
    - `setup_capabilities.sh:44-52,85-98` already VERIFY-not-install for mlx (Phase 1 D-02/D-05: no venv, no pip). Phase 5 does NOT add a pip install (Out-of-Scope "a second Yulu-specific venv") — the gate only changes the MESSAGE (reuse vs absent), not the install behavior.
    - Mapping: whisper-cpp -> `whisper_cli`; gogcli -> `gog` (Task 1); mlx -> `mlx_whisper`. A doctor error -> `absent` -> install (never over-skip).
  </read_first>
  <files>yulu/scripts/lib/common.sh, yulu/scripts/setup_deps.sh, yulu/scripts/setup_capabilities.sh</files>
  <action>
    Add `capability_status()` to lib/common.sh: takes a capability name, runs `doctor.py --json` via PYTHON_BIN (line-124 resolution) from $SCRIPT_DIR, pipes stdout into a `"$PYTHON_BIN" -c` one-liner that loads JSON and prints `host_capabilities.capabilities.<cap>.status` defaulting to `absent`, echoing `absent` on any failure. Never interpolate resolved_path into a shell command. In setup_deps.sh: when `capability_status whisper_cli` equals usable, install only sox/ffmpeg/terminal-notifier plus an ok reuse message, else install the full set including whisper-cpp; gate gogcli the same way on `capability_status gog`; leave cloudflared unconditional. In setup_capabilities.sh: when `capability_status mlx_whisper` equals usable, emit the reuse ok message and skip; else keep the existing advisory warn (NO pip install). Add comments citing D-04, D-05, Pitfall 4. Keep set -uo pipefail, idempotent, non-interactive.
  </action>
  <verify>
    <automated>bash -n yulu/scripts/lib/common.sh; bash -n yulu/scripts/setup_deps.sh; bash -n yulu/scripts/setup_capabilities.sh; grep -q capability_status yulu/scripts/lib/common.sh</automated>
  </verify>
  <done>capability_status() exists; setup_deps.sh skips whisper-cpp and gogcli when their tri-state is usable, installs otherwise; setup_capabilities.sh emits the reuse message when mlx_whisper is usable; no boolean-collapse test present; all three pass bash -n; no pip install added.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Wave-0 parametrized reuse-gate test (REUSE-01/02)</name>
  <read_first>
    - RESEARCH Validation Wave 0: `tests/test_reuse_gating.py` covers REUSE-01/02 (parametrized tri-state -> skip/install). Must run on ANY OS (mock the report; no real doctor/daemon).
    - STATE.md [04-01]: tests live at repo-root `tests/` (make pytest = pytest tests), NOT yulu/scripts/tests/.
    - The unit under test is the DECISION at the report boundary: the same `host_capabilities.capabilities.<cap>.status` lookup capability_status does. Feed a fabricated report dict (report.py to_dict() shape: {schema_version, capabilities:{<cap>:{provenance,status,resolved_path,detail}}}) into a tiny status-extraction helper; assert skip/install.
    - tri-state values: usable / present-but-unverified / absent. Decision: usable -> skip; the other two -> install. Cover whisper_cli, mlx_whisper, gog; plus a missing-key / malformed-report case (-> absent -> install).
  </read_first>
  <files>tests/test_reuse_gating.py</files>
  <behavior>
    - Parametrized over usable/present-but-unverified/absent: the status-extraction helper returns the exact status for whisper_cli/mlx_whisper/gog from a fabricated report.
    - The decision is `skip` ONLY when status == usable; present-but-unverified and absent both -> install (Pitfall 4).
    - A report missing the capability key, or missing host_capabilities, yields absent -> install (safe default).
    - A gog entry usable with no version detail still -> skip (resolution, not version, drives usability).
  </behavior>
  <action>
    Create tests/test_reuse_gating.py at repo root. Self-contained: replicate the `host_capabilities.capabilities.<cap>.status` lookup as a `.get(...)` chain defaulting to absent, then `decision = "skip" if status == "usable" else "install"`. Parametrize the three tri-state values across whisper_cli/mlx_whisper/gog; add missing-key/malformed cases. Build reports via `capabilities.report.HostCapabilityReport(...).to_dict()` (insert yulu/scripts on sys.path, doctor-test idiom) or an equivalent dict literal (shape is frozen). Assert decisions match the table. This is the executable proof of D-04 + Pitfall 4.
  </action>
  <verify>
    <automated>python3 -m pytest tests/test_reuse_gating.py -x -q</automated>
  </verify>
  <done>tests/test_reuse_gating.py green; parametrizes all three tri-state values across whisper_cli/mlx_whisper/gog; proves only usable -> skip and missing/malformed -> install.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| doctor.py --json -> bash setup scripts | A capability's resolved_path/detail crosses into shell scripts |
| host login PATH -> probe_command("gog") | Resolves a host binary name; never executes free-form input |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-04 | Tampering / Command injection | Reuse gate reading a capability path into bash | mitigate | capability_status runs doctor.py --json with FIXED argv and parses JSON in Python, printing only `status`; never interpolates resolved_path into a shell command (probes.py T-03-02 discipline). |
| T-05-05 | Spoofing / silent failure | A broken host whisper reported "present" -> over-skip | mitigate | Gate strictly on status == usable (Pitfall 4); present-but-unverified falls through to install. The Phase-3 probe verifies actual usability, not mere presence. |
| T-05-06 | DoS | A hung/slow doctor blocking setup | accept | Install-time flow, not a request handler; a slow doctor degrades to absent -> install (safe). No timeout added (the Phase-4 UI router already SIGKILLs doctor, T-04-DOS). |
| T-05-SC | Tampering | npm/pip/cargo installs | mitigate | This plan REDUCES installs (gates brew install whisper-cpp) and adds NO new package; slopcheck N/A per RESEARCH Package Legitimacy Audit; no [ASSUMED]/[SUS] package, so no blocking-human checkpoint required. |
</threat_model>

<verification>
- `make pytest` green (includes tests/test_reuse_gating.py).
- `bash -n` passes on lib/common.sh, setup_deps.sh, setup_capabilities.sh.
- `python3 yulu/scripts/doctor.py --json | python3 -m json.tool` shows a gog key under host_capabilities.capabilities.
- `grep -n capability_status yulu/scripts/setup_deps.sh yulu/scripts/setup_capabilities.sh` shows the gate in both.
- `grep -nE '\-n "\$status"|!= "absent"' yulu/scripts/setup_deps.sh yulu/scripts/setup_capabilities.sh` returns nothing (no boolean collapse).
</verification>

<success_criteria>
- REUSE-01: a usable host whisper-cli / mlx-whisper / claude / gog is reused; gog is now in the report so its install is gated.
- REUSE-02: no unconditional brew install whisper-cpp; no duplicate MLX venv (no pip install added).
- D-04: the tri-state (not a boolean) gates the decision — only usable skips.
- D-05: never silently mutate the host package manager; the rest of the brew set still installs.
- Runs in Wave 1 alongside Plans 01 and 03.
</success_criteria>

<output>
Create `.planning/phases/05-capability-reuse-data-folder-cloud-sync-safety/05-02-SUMMARY.md` when done.
</output>
