---
phase: 09-release-safety
plan: 03
type: execute
wave: 2
depends_on: [09-01, 09-02]
files_modified:
  - yulu/scripts/setup.sh
  - yulu/scripts/setup_deps.sh
  - yulu/scripts/provision/registry.py
  - tests/test_setup_decomposition.py
  - tests/test_provision_registry.py
  - tests/test_package_release.py
autonomous: false
requirements: [DIST-04]

must_haves:
  truths:
    - "Core setup succeeds when ffmpeg, sox, and a compatible Node are usable even if Homebrew, Hermes, OpenClaw, gog, cloudflared, and terminal-notifier are absent (DIST-04)."
    - "Setup never downloads or executes the Homebrew installer; when a required core command is missing and no package manager exists, it stops with an actionable message instead of mutating the host (DIST-04)."
    - "Hermes, Codex, Claude, and OpenClaw MCP registration is detected-only and non-fatal; no one Agent is selected or required by core setup (DIST-04)."
    - "gog/cloudflared installation and the calendar LaunchAgent remain behind the existing exact `YULU_INSTALL_CALENDAR=1` opt-in; fresh core setup defaults to defer (DIST-04)."
    - "The provision `deps` readiness probe matches the core postcondition exactly, so an intentionally deferred optional capability does not cause endless reruns (DIST-04)."
    - "Phase 9 cannot complete until one exact signed draft/pre-release candidate passes live stable-channel/dev refusal, legacy staged-guard, and clean-host core-install verification; macOS 13 arm64 hardware acceptance is waived while the five-binary `vtool` gate remains required, and this does not publish or accept public latest stable."
  artifacts:
    - path: "yulu/scripts/setup.sh"
      provides: "Read-only Homebrew preflight, optional Agent registration, defer-by-default calendar prompt"
      contains: "--detected-only --non-fatal"
    - path: "yulu/scripts/setup_deps.sh"
      provides: "Core-first dependency postcondition with calendar-only optional installs"
      contains: "YULU_INSTALL_CALENDAR"
    - path: "yulu/scripts/provision/registry.py"
      provides: "Core-only `_deps_ready` probe"
      contains: "def _deps_ready"
    - path: "tests/test_setup_decomposition.py"
      provides: "Hermetic no-brew/no-Agent/no-calendar core success and explicit calendar opt-in tests"
      contains: "core"
    - path: "tests/test_provision_registry.py"
      provides: "Core readiness excludes optional commands"
      contains: "test_deps_probe"
    - path: "tests/test_package_release.py"
      provides: "Release setup contract no longer asserts mandatory Hermes"
      contains: "mcp"
    - path: ".planning/phases/09-release-safety/09-03-SUMMARY.md"
      provides: "Traceable candidate tag/workflow/hash and real-host checkpoint evidence"
      contains: "09-03-03"
  key_links:
    - from: "yulu/scripts/setup.sh"
      to: "yulu/scripts/setup_deps.sh"
      via: "existing explicit dependency-consent orchestration; check_system remains read-only"
      pattern: "run_setup_deps"
    - from: "yulu/scripts/setup_deps.sh"
      to: "YULU_INSTALL_CALENDAR=1"
      via: "exact existing optional-capability gate for gog/cloudflared"
      pattern: "YULU_INSTALL_CALENDAR:-.*[!=]=.*1"
    - from: "yulu/scripts/provision/registry.py"
      to: "setup_deps.sh core postcondition"
      via: "ffmpeg + sox + compatible Node only"
      pattern: "required_commands.*ffmpeg.*sox"
    - from: ".planning/phases/09-release-safety/09-03-SUMMARY.md"
      to: "signed draft/pre-release candidate"
      via: "09-03-03 records one candidate tag, workflow URL, asset hashes, and three evidence groups"
      pattern: "candidate.*(tag|workflow|hash)"
---

<objective>
Make installation mean “Yulu recording core is ready,” not “every optional Agent/calendar integration was installed.”

Purpose: Remove silent host mutation and optional-service hard failures while keeping the existing setup/provision structure and calendar opt-in seam.
Output: Read-only Homebrew preflight, core-only dependency install/readiness, non-fatal Agent registration, explicit calendar gating, hermetic tests, and one unified blocking signed-candidate/real-host acceptance record.
</objective>

<execution_context>
@/Users/liaoyuxing/.codex/get-shit-done/workflows/execute-plan.md
@/Users/liaoyuxing/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/09-release-safety/09-RESEARCH.md
@.planning/phases/09-release-safety/09-PATTERNS.md
@.planning/phases/09-release-safety/09-VALIDATION.md
@.planning/phases/01-build-foundation-setup-decomposition-signed-notarized-binari/01-02-SUMMARY.md
@.planning/phases/06-agent-orchestrated-provisioning-decoupled-skill-install/06-01-SUMMARY.md
@.planning/phases/06-agent-orchestrated-provisioning-decoupled-skill-install/06-04-SUMMARY.md

@yulu/scripts/setup.sh
@yulu/scripts/setup_deps.sh
@yulu/scripts/setup_daemons.sh
@yulu/scripts/lib/common.sh
@yulu/scripts/provision/registry.py
@tests/test_setup_decomposition.py
@tests/test_provision_registry.py
@tests/test_package_release.py

<interfaces>
Existing seams to preserve:

- `setup.sh` is the only interactive orchestrator and owns `confirm_deps_install`, `setup_calendar`, and `confirm_calendar_plist`; concern scripts remain non-interactive/idempotent.
- `setup_deps.sh::ensure_brew_command(formula, command_name)` keeps postcondition-over-exit behavior for explicitly consented installs.
- `lib/common.sh::compatible_node_bin` is the one Node compatibility policy; do not duplicate version parsing.
- `setup_daemons.sh` installs the calendar service only when `YULU_INSTALL_CALENDAR=1`, or refreshes a previously installed calendar plist on upgrade. Do not edit or replace that seam.
- `provision.registry._deps_ready()` must express the same resulting core state as `setup_deps.sh`, because `ScriptStep.apply()` skips only when that probe is true.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Make dependency setup core-first and Homebrew non-automatic</name>
  <files>yulu/scripts/setup.sh, yulu/scripts/setup_deps.sh, yulu/scripts/provision/registry.py, tests/test_setup_decomposition.py, tests/test_provision_registry.py</files>
  <read_first>
    - `yulu/scripts/setup.sh:106-169,613-650` — remove only automatic Homebrew bootstrap from `check_system`; preserve the orchestrator-owned dependency consent and concern sequencing.
    - `yulu/scripts/setup_deps.sh:19-105` — keep `ensure_brew_command` and compatible Node policy; split core postcondition from calendar-only work without another registry.
    - `yulu/scripts/provision/registry.py:174-224` — `_compatible_node_present` is the accepted Node probe; narrow `_deps_ready` to exactly the core postcondition and preserve never-raise/read-only behavior.
    - `tests/test_setup_decomposition.py:45-135,201-262,365-431` — reuse fake PATH, throwaway HOME, call logging, repeated-run, and no-network patterns.
    - `tests/test_provision_registry.py:111-139,218-291` — update the existing `_deps_ready` test; preserve argv-list/no-shell and ScriptStep contracts.
    - `.planning/phases/09-release-safety/09-RESEARCH.md` Pattern 4 and Open Question 3 — remove only proven optional blockers; retain ffmpeg, sox, and compatible Node as core in this phase.
  </read_first>
  <behavior>
    - `check_system()` only reports whether Homebrew exists; it never curls/executes the Homebrew installer and does not fail merely because brew is absent.
    - `setup_deps.sh` returns success without invoking brew when ffmpeg, sox, and a compatible Node are already usable; terminal-notifier is not a core postcondition.
    - If a core dependency is missing, only an explicitly invoked/consented dependency step may use an already-installed brew; missing brew returns a clear non-zero instruction and never bootstraps it.
    - `gog` and `cloudflared` are not installed or required unless the exact existing opt-in `YULU_INSTALL_CALENDAR=1` is present.
    - `_deps_ready()` is true for ffmpeg + sox + compatible Node with brew/gog/cloudflared/terminal-notifier absent, and false when any core command or Node compatibility is missing.
    - Both default core and explicit calendar-opt-in dependency runs are idempotent; all brew formula and command names remain fixed literals.
  </behavior>
  <action>
    Add failing hermetic tests first. In `test_setup_decomposition.py`, construct a PATH containing usable ffmpeg/sox/Node but no brew, Agents, gog, cloudflared, or terminal-notifier; run `setup_deps.sh release` twice and require success with no network/package-manager marker. Add an explicit `YULU_INSTALL_CALENDAR=1` case with a fake brew log to prove only that opt-in permits fixed gog/cloudflared install attempts. In `test_provision_registry.py`, reduce the accepted postcondition to ffmpeg, sox, and compatible Node and prove each missing core item fails.

    In `setup.sh::check_system`, delete the remote Homebrew installation and replace it with read-only status/instructions. Do not call curl, sudo, `/bin/bash -c`, or another bootstrap. Keep Python/macOS checks and the existing explicit dependency confirmation boundary.

    In `setup_deps.sh`, define/reuse one small core postcondition using `command -v` plus `compatible_node_bin`. When it is satisfied, skip all core brew work; if calendar opt-in is absent, return success, while an explicit `YULU_INSTALL_CALENDAR=1` continues to the separately gated optional block. For missing core commands, retain `ensure_brew_command` and Node postcondition checks but require an already-installed brew and fail actionably otherwise. Remove terminal-notifier, gog, and cloudflared from core install/failure. Put existing fixed gog/cloudflared installs only behind exact `YULU_INSTALL_CALENDAR=1`; do not create a new optional dependency registry or prompt inside the concern script.

    In `provision/registry.py::_deps_ready`, require only `ffmpeg`, `sox`, and `_compatible_node_present()`. Keep the probe read-only, never-raise through `ScriptStep.check`, and do not execute detected commands beyond the existing Node version probe.
  </action>
  <acceptance_criteria>
    - Core-ready/no-brew dependency run succeeds twice and records zero brew/curl/Homebrew-bootstrap activity.
    - Missing core dependency + missing brew fails clearly without changing the host.
    - Default dependency run never names/installs/requires gog, cloudflared, terminal-notifier, Hermes, or OpenClaw.
    - Exact `YULU_INSTALL_CALENDAR=1` is the only path in this task that permits fixed gog/cloudflared install attempts.
    - `_deps_ready()` and `setup_deps.sh` agree on ffmpeg + sox + compatible Node; intentionally deferred optional tools do not cause re-run.
    - Existing Node-version policy, `ensure_brew_command` postcondition behavior, concern idempotency, and argv-list ScriptStep execution stay green.
  </acceptance_criteria>
  <verify>
    <automated>cd "$(git rev-parse --show-toplevel)" &amp;&amp; python3 -m pytest -q tests/test_setup_decomposition.py tests/test_provision_registry.py &amp;&amp; bash -n yulu/scripts/setup.sh yulu/scripts/setup_deps.sh &amp;&amp; shellcheck -x -P SCRIPTDIR yulu/scripts/setup.sh yulu/scripts/setup_deps.sh</automated>
  </verify>
  <done>Core dependency readiness is ffmpeg + sox + compatible Node, a ready host needs no brew, absent brew is never auto-installed, and calendar tooling runs only on the existing explicit opt-in.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Make Agents and calendar activation non-blocking in core setup</name>
  <files>yulu/scripts/setup.sh, tests/test_setup_decomposition.py, tests/test_package_release.py</files>
  <read_first>
    - `yulu/scripts/setup.sh:269-383` — `setup_calendar` already describes itself as optional; make fresh calendar service choice defer-by-default and retain `YULU_INSTALL_CALENDAR=1` only after explicit consent.
    - `yulu/scripts/setup.sh:651-670` — replace the Hermes-required branch with the existing `--detected-only --non-fatal` pattern for all four Agents; this phase does not choose a provider.
    - `yulu/scripts/setup.sh:285-320` — safe `~` expansion already occurs before the redundant `eval echo`; remove the eval so optional calendar input cannot become shell code.
    - `tests/test_package_release.py:286-310` — update the existing release setup ordering test: MCP registration remains before Host start, but all four Agents are detected-only/non-fatal and no Hermes-required copy remains.
    - `tests/test_setup_decomposition.py:45-135` — use a fake `provision.cli`/PATH and no live Agent, calendar OAuth, launchctl, or network.
  </read_first>
  <behavior>
    - Core setup invokes one MCP registration command covering Hermes, Codex, Claude, and OpenClaw with `--detected-only --non-fatal`; zero detected Agents is a successful install state.
    - No setup error or exit says Hermes/OpenClaw/another Agent is required; setup does not select a summary/conversation provider.
    - A fresh calendar service prompt defaults to defer (`[y/N]` semantics); only an explicit yes exports `YULU_INSTALL_CALENDAR=1`; upgrade preserves an already installed calendar service through `setup_daemons.sh`.
    - Calendar credential-path handling performs the existing literal leading-tilde expansion only and executes no user-provided shell syntax.
    - MCP registration still occurs before Host/daemon startup, preserving the existing endpoint-registration ordering.
  </behavior>
  <action>
    Add/update tests first. Change the static release contract in `tests/test_package_release.py` to assert a single all-Agent registration with Hermes/Codex/Claude/OpenClaw, `--detected-only --non-fatal`, before `run_setup_concerns`, and absence of the Hermes-required error. Add a hermetic setup test where all Agent CLIs and calendar tools are absent and the mocked MCP command returns its non-fatal result; core orchestration must proceed. Add a prompt-default test proving an empty answer does not set `YULU_INSTALL_CALENDAR`, and an adversarial credential-path test/source assertion proving no `eval` remains.

    In `setup.sh`, replace the required Hermes invocation plus optional other invocation with one detected-only/non-fatal registration for all four known Agents. Treat a warning as a warning and continue; do not add detection logic, install an Agent, select a provider, or modify credentials/config for Phase 10+.

    Change `confirm_calendar_plist` to defer by default and set `YULU_INSTALL_CALENDAR=1` only for explicit yes. Preserve `setup_calendar`'s existing explicit yes path and `setup_daemons.sh`'s upgrade inheritance. Remove the redundant `cred_path="$(eval echo "$cred_path")"`; the immediately preceding literal `~` expansion is sufficient, and no user path should be executed as shell.
  </action>
  <acceptance_criteria>
    - Setup source and hermetic test prove all four Agents share one detected-only/non-fatal command before Host start.
    - No required-Hermes text/exit remains; no Agent absence blocks core install and no provider is selected by setup.
    - Empty fresh calendar response defers; explicit yes is required for `YULU_INSTALL_CALENDAR=1`; upgrade inheritance remains in the untouched `setup_daemons.sh`.
    - No `eval` or dynamically built shell command handles the calendar credential path; an adversarial path executes no marker.
    - Existing setup concern sequencing, CLI install, migration tail, run-tests warning behavior, and config permissions remain green.
  </acceptance_criteria>
  <verify>
    <automated>cd "$(git rev-parse --show-toplevel)" &amp;&amp; python3 -m pytest -q tests/test_setup_decomposition.py tests/test_provision_registry.py tests/test_package_release.py -k 'setup or deps or provision or mcp or calendar or package' &amp;&amp; ! grep -q 'Hermes CLI and its Yulu phase MCP registrations are required' yulu/scripts/setup.sh &amp;&amp; ! grep -q 'eval echo' yulu/scripts/setup.sh</automated>
  </verify>
  <done>Agent registration and calendar activation are optional, defer-safe, and non-injectable; core setup proceeds without Hermes/OpenClaw/calendar tooling while keeping existing setup ordering and opt-in seams.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 3: Accept the signed release candidate on real release, live-recording, and clean-host boundaries</name>
  <read_first>
    - `.planning/phases/09-release-safety/09-VALIDATION.md` — follow the 09-03-03 manual-only matrix exactly and record evidence for all three checks.
    - `.planning/phases/09-release-safety/09-RESEARCH.md` Resolved Open Questions and Manual Acceptance — legacy release updates use the new Release installer plus verified staged guard; core dependencies remain ffmpeg, sox, and compatible Node. The user has waived macOS 13 arm64 hardware acceptance for v0.6.
    - `.planning/phases/09-release-safety/09-01-SUMMARY.md` — confirm the version-paired installer and shared active-recording guard automated gates completed before live verification.
    - `.planning/phases/09-release-safety/09-02-SUMMARY.md` — confirm all five final-artifact deployment-target gates completed; this automated gate remains required despite the hardware-test waiver.
    - `yulu/scripts/setup.sh`, `yulu/scripts/setup_deps.sh`, `yulu/scripts/provision/registry.py`, `tests/test_setup_decomposition.py`, and `tests/test_provision_registry.py` — inspect the completed Task 1-2 implementation and passing evidence before clean-host verification; this plan's SUMMARY does not exist until the checkpoint completes.
  </read_first>
  <what-built>
    A version-paired stable-channel release-candidate installer, one canonical recording-active refusal path shared by release and dev updates, an explicit macOS 13 target plus final-artifact `vtool` gate for all five shipped Swift binaries, and a core install boundary that does not require or silently install Homebrew, Hermes/OpenClaw, or calendar tooling. Tasks 09-01-01 through 09-03-02 provide code/hermetic evidence; this checkpoint covers only the real signed asset, live daemon/WAV, and clean-host state that fixtures cannot prove. macOS 13 arm64 hardware acceptance is waived, not marked PASS.
  </what-built>
  <how-to-verify>
    PREREQUISITE: Tasks 09-01-01 through 09-03-02 and their automated commands pass. Produce a real signed/notarized candidate through the credentialed release workflow, leaving the public latest-stable release unchanged. It may remain draft for exact-byte workflow checks; if the installer's release-by-tag download needs public asset access for steps 2-3, publish only as a GitHub pre-release, never as a stable/latest release. Record the candidate tag, workflow run, checksums, and downloaded asset identities.

    1. Signed candidate integrity: download the exact candidate `install.sh`, runtime zip, and checksums from the draft or pre-release. Confirm workflow signing/notarization, manifest, checksum, attestation, remote-byte comparison, and the five-binary deployment-target gate all succeeded for those exact bytes. Confirm packaged `install.sh` embeds/pins the same candidate tag. Draft evidence is sufficient for this step; use pre-release status only when end-to-end tag download requires it, and never promote the candidate to public latest stable for this checkpoint.
    2. Live recording refusal: with a real recording active, capture runtime VERSION, daemon PIDs, WAV path, and size. Run the candidate's version-pinned stable-channel update/reinstall, then the dev update path. Each must refuse non-zero before mutation/setup, preserve VERSION/metadata/PIDs, leave the WAV growing, and produce a valid WAV after an explicit user stop. Exercise a legacy pre-v0.6 fixture through the new candidate installer and confirm the checksum/signature/manifest-verified staged guard refuses before swap when recording is active.
    3. Clean-host core install: in a clean user/VM with no Homebrew, Hermes, OpenClaw, gog, cloudflared, or terminal-notifier, but with ffmpeg, sox, and a compatible Node available, run the exact candidate core install. Confirm setup succeeds, core UI/daemons start, no Homebrew bootstrap/package-manager mutation occurs, zero Agent absence is non-fatal, calendar defaults to deferred, and no calendar tooling/service is installed without exact `YULU_INSTALL_CALENDAR=1` opt-in.
  </how-to-verify>
  <action>Pause after all six autonomous tasks are green and ask the human to execute steps 1-3 against the exact signed draft/pre-release candidate. Record candidate tag/workflow/asset hashes and observed PID/WAV/host-state evidence in the execution summary. Record macOS 13 arm64 hardware acceptance as waived, never PASS. Do not publish or claim acceptance of the public `releases/latest` stable endpoint: Phase 13 DOCS-03 owns the post-release latest-stable smoke and documentation alignment. Resume only when every in-scope step is approved; any failed high-risk check blocks Phase 9 completion.</action>
  <files>(none — this is a blocking human-verification gate; Tasks 1-2 and Plans 09-01/09-02 own all implementation files)</files>
  <verify>
    <human-check>All three in-scope real checks pass against one traceable signed draft/pre-release candidate: exact asset integrity/version pairing, live stable-channel/dev refusal including legacy staged guard, and clean-host core install without optional tools or silent Homebrew mutation. macOS 13 arm64 hardware acceptance is recorded as waived, public latest stable remains unchanged and unclaimed.</human-check>
  </verify>
  <acceptance_criteria>
    - One candidate tag, credentialed workflow run, and exact downloaded asset hashes connect all release-candidate observations; signing/notarization/attestation/checksum/manifest and five-binary `vtool` gates apply to those bytes.
    - Candidate version-pinned stable-channel and dev updates both refuse during a live recording with unchanged runtime/metadata/PIDs and an intact WAV; the legacy release fixture proves verified staged guard before swap.
    - Core setup succeeds with only ffmpeg, sox, and compatible Node present; it performs no optional Agent/calendar/Homebrew mutation by default.
    - The candidate remains draft/pre-release or otherwise does not replace public latest stable; Phase 13 DOCS-03 remains responsible for public latest-stable post-release smoke.
  </acceptance_criteria>
  <resume-signal>Reply `approved` with the candidate tag/workflow URL and evidence for checks 1-3, or describe the failed check. Any failure blocks completion and returns to the owning implementation task.</resume-signal>
  <done>The reviewer approves all three in-scope evidence groups for one signed draft/pre-release candidate; macOS 13 arm64 hardware acceptance is recorded as waived, failures remain blocking, and public latest-stable acceptance is still deferred to Phase 13 DOCS-03.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| setup preflight → host package manager | Core install must not silently download/execute Homebrew or mutate system tooling without explicit action. |
| `YULU_INSTALL_CALENDAR` → optional package/service mutation | Only the existing exact opt-in may permit calendar tooling and LaunchAgent work. |
| user calendar path → shell/filesystem | A user-provided path is data, not shell code. |
| detected Agent state → MCP registration | Absence or failure of an optional runtime must not become a core-install denial or secret/config mutation. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09-03-01 | Elevation of Privilege / supply chain | automatic Homebrew bootstrap in `check_system` | mitigate (HIGH, blocking) | Remove remote installer execution entirely; preflight is read-only. Only an explicit dependency step may use an already-installed brew with fixed formula names. |
| T-09-03-02 | Tampering / silent host mutation | gog/cloudflared/calendar service | mitigate | Exact `YULU_INSTALL_CALENDAR=1` gate, defer-by-default prompt, fixed formula argv, and hermetic call-log tests proving zero optional work by default. |
| T-09-03-03 | Tampering / shell injection | calendar credential path and dependency commands | mitigate (HIGH, blocking) | Delete redundant `eval echo`; keep literal tilde expansion, fixed formula/command names, quoted variables, and adversarial marker test. Do not add shell-built Agent commands. |
| T-09-03-04 | Denial of Service | optional Agent absence and mismatched deps probe | mitigate | One detected-only/non-fatal all-Agent registration; synchronize `setup_deps` and `_deps_ready` on core postconditions so deferred tools neither fail nor endlessly rerun. |
| T-09-03-SC | Tampering | existing Homebrew formula installs | mitigate | No new formula/package is introduced. Existing fixed core formula paths remain behind explicit setup/provision action and postcondition checks; optional formulae require explicit calendar opt-in. Research Package Legitimacy Audit therefore requires no new package checkpoint. |
</threat_model>

<source_coverage>
| Source | ID | Item | Status |
|--------|----|------|--------|
| GOAL | — | Core install has no forced optional integrations or automatic Homebrew | COVERED by Tasks 1-2 |
| REQ | DIST-04 | Install without Hermes/OpenClaw/calendar/Homebrew | COVERED by Tasks 1-2 |
| RESEARCH | Pattern 4 | Core-only dependency postcondition and detected-only Agent registration | COVERED by Tasks 1-2 |
| RESEARCH | Pitfalls 7-9 | Probe parity, no-brew core-ready host, no mandatory Hermes | COVERED by tests and synchronized changes |
| CONTEXT | — | No Phase 09 CONTEXT.md or D-XX decisions; PROJECT makes Agents optional and Phase 13 owns calendar UX | COVERED without provider/onboarding redesign |
</source_coverage>

<verification>
Automated plan gate:

1. `python3 -m pytest -q tests/test_setup_decomposition.py tests/test_provision_registry.py tests/test_package_release.py`
2. `bash -n yulu/scripts/setup.sh yulu/scripts/setup_deps.sh`
3. `shellcheck -x -P SCRIPTDIR yulu/scripts/setup.sh yulu/scripts/setup_deps.sh`
4. Static gates find no Homebrew bootstrap URL, no required-Hermes error, and no `eval echo` in executable setup source.

<human-check>
Blocking checkpoint 09-03-03 unifies the signed candidate, live-recording refusal, legacy staged-guard, and clean-host core-install evidence. macOS 13 arm64 hardware acceptance is explicitly waived rather than marked PASS; the automated five-binary `vtool` gate remains mandatory. Pytest shims are not a substitute for the remaining external-state checks, and public latest-stable publication/smoke remains Phase 13 DOCS-03.
</human-check>
</verification>

<success_criteria>
- DIST-04 is met in code and hermetic tests: core install succeeds with optional Agents/calendar tools/Homebrew absent when core commands are ready.
- No automatic package-manager bootstrap, mandatory Agent registration, default calendar activation, or optional-tool readiness loop remains.
- The existing setup decomposition, provision registry, Node policy, calendar opt-in seam, config permissions, and lifecycle order remain intact.
- Signed-candidate, live-recording, and clean-host acceptance remain pending until blocking checkpoint 09-03-03 records all in-scope evidence; macOS 13 arm64 hardware acceptance is waived while its five-binary automated gate remains required, and public latest-stable smoke is not part of Phase 9.
</success_criteria>

<output>
Create `.planning/phases/09-release-safety/09-03-SUMMARY.md` when done.
</output>
