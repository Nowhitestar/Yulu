# 260625-010 Agent Console UI Prune, Zulip Detection, Sidebar Workbench

## Goal

Respond to the user's product cleanup request:

1. Audit remaining superficial Agent Console UI.
2. Fix Codex Zulip capability detection when the local Codex CLI can use Zulip.
3. Remove the Settings page Agent Console runtime/summary/capability explainer block.
4. Fold recordings, templates, and glossary into the workbench navigation.
5. Remove "Inbox" and "Knowledge Base" wording from the sidebar.

## Assumptions

- Agent Console is the primary workbench surface.
- Settings should stop explaining migrated AI runtime concepts.
- Templates and glossary remain real routes, but their sidebar placement should be under workbench.
- Zulip configured in Codex can be detected via Agent plugin roots or Codex MCP/tool configuration, not only by a directory named `zulip`.

## Verification

- `npm run typecheck`
- Focused web/router tests for sidebar, settings, Agent Console plugin detection
- `npm run build`
- `make dev-install`
- `python3 yulu/scripts/doctor.py --json`

## Result

Completed 2026-06-25.

- Removed the visible Settings Agent Console / LLM migration entry and legacy `/settings/llm` now redirects to Agent Console.
- Merged recordings, templates, and glossary into the Workspace sidebar section.
- Changed user-facing "提示词" navigation copy to "模板".
- Hid Agent/LLM-managed capabilities from Settings host capabilities.
- Fixed Codex Zulip plugin detection by reading Codex MCP server entries from `~/.codex/config.toml`.
- Verified with typecheck, focused tests, full Vitest, production build, dev-install, doctor, and a live Codex/Zulip overview probe.
