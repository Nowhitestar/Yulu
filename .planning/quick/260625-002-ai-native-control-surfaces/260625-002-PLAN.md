---
status: completed
date: 2026-06-25
---

# Quick Task 260625-002: AI Native Control Surfaces

## Goal

Turn the remaining audit findings into usable control surfaces without changing the core recording/STT architecture.

## Tasks

1. Agent Queue Console
   - Add a tRPC queue router around `agent-queue.json`.
   - Add a UI view to inspect queue status, errors, prompt snapshots, and retry/cancel stale items.
   - Verify with focused router/web tests.

2. Doctor Dashboard
   - Add a tRPC doctor router that wraps the existing `doctor.py --json` path and composes existing health signals.
   - Add a Health/Doctor page so users do not need to stitch together Settings, Logs, and daemon health manually.
   - Verify with focused tests.

3. Scheduler/Calendar Console
   - Add a read-only schedule router over `schedule.json` and calendar service status.
   - Add a UI view for upcoming events/provider state/reload.
   - Verify with focused tests.

4. Residual Cleanup
   - Keep `search.doctor`/daemon start/file output/legacy components either surfaced or explicitly documented as deferred.
   - Avoid speculative rewrites and keep changes scoped to visible control surfaces.

## Result

Completed 2026-06-25. Added Health control surfaces for Doctor, Agent Queue, and Scheduler, plus daemon start controls and the Agent-queue preferred Agent CLI setting.
