---
status: completed
date: 2026-06-25
---

# Quick Task 260625-002 Summary

## Shipped

- Added Health tabs for Doctor, Agent Queue, and Scheduler.
- Added tRPC routers for `doctor.run`, `queue.list/retry/cancel/clearStale`, and `scheduler.overview/reload`.
- Surfaced daemon start controls for stopped daemons.
- Surfaced `llm.agent.provider` in LLM settings so Agent-queue mode can prefer auto/Codex/Claude Code.

## Verification

- `npm run typecheck`
- `npm run test -- tests/routers/queue.test.ts tests/routers/scheduler.test.ts tests/routers/daemons.test.ts tests/web/DaemonCard.test.tsx tests/web/routes/health.test.tsx tests/web/routes.test.tsx tests/web/LlmSection.test.tsx tests/settingsRegistry.test.ts`
- `npm run build`
- `YULU_CODESIGN_IDENTITY=- make dev-install`
- `python3 yulu/scripts/doctor.py --json`
- HTTP smoke: `http://127.0.0.1:7777/healthz` and `http://127.0.0.1:7777/health` returned 200.

## Notes

- The first `make dev-install` attempt failed because Apple timestamp service was unavailable during Developer ID signing. The local install was completed with ad-hoc signing via `YULU_CODESIGN_IDENTITY=-`.
- A transient post-reload `micReady:false` recovered; final doctor reports audio socket ok with `recording:false`, `sysReady:true`, and `micReady:true`.
