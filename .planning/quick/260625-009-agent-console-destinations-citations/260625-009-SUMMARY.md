# 260625-009 Summary

## Implemented

- Added Agent-scoped Console destinations under `agent_console.destinations.<agent>`.
- Added visible Agent detection feedback in Agent Console.
- Added Notion/Zulip destination editing from Current Capabilities.
- Routed summary send actions through the selected Agent destination hint.
- Kept configured Zulip disabled until both Channel and Topic are selected.
- Changed Ask Meetings so local citations are returned only when the answer cites `[N]`.
- Added remote Agent context chips for Notion/Zulip/Calendar instead of mixing them into local citations.
- Clarified the calendar split: Agent calendar context is for AI context; Yulu local Scheduler still owns deterministic reminders and auto-recording.

## Verification

- `npm run typecheck` passed.
- Focused Vitest passed: 7 files, 103 tests.
- Full Vitest passed under Node 20.17.0 with local socket access: 102 files, 674 tests.
- `npm run build` passed.
- `make dev-install` succeeded.
- `python3 yulu/scripts/doctor.py --json` showed UI healthz, audio socket, STT daemon, and search index healthy.
- Local UI healthz checked on both `127.0.0.1:7777` and `127.0.0.1:7780`.

## Remaining Gap

- Agent calendar context is not yet the sole schedule source. The local Scheduler still reads local calendar sources because it must deterministically trigger reminders and recordings. A later migration can let the selected Agent provider refresh `schedule.json`, then remove the duplicate local provider UI.
