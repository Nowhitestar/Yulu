# 260625-009 Agent Console Destinations And Citations

## Goal

Finish the last Agent Console migration layer requested by the user:

1. Make the underlying Agent detection button visibly do work.
2. Move Notion/Zulip send destinations into Agent-scoped Console preferences.
3. Clarify Agent calendar context versus Yulu local scheduling.
4. Make Ask Meetings citations less aggressive and internally consistent.

## Success Criteria

- Agent Console detection shows a running/done state and refreshed detected paths.
- Current capabilities can configure Notion and Zulip destinations per selected Agent.
- Sending summaries passes the selected Agent-scoped destination hints to the Agent CLI.
- Ask Meetings only displays numbered local citations when the answer cites them inline.
- Remote Agent connector context is not mislabeled as local meeting citations.
- Settings copy makes clear that local scheduling still owns deterministic recording/reminders.

## Verification

- `npm run typecheck`
- Focused Vitest coverage for `agentConsole`, `ask`, `recordings`, Agent Console route, and settings integrations
- `npm run build`
- `make dev-install`
- `python3 yulu/scripts/doctor.py --json`
