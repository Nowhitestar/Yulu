# 260805-0f7 A2 Dictation Overlay

## Goal

Implement the approved A2 dictation overlay while preserving the current dictation flow and result-driven feedback.

## Task

1. Update `yulu/scripts/status_agent.swift` to use the approved 180/140/112pt state widths, center-anchored resizing, compact centered layout, electric angular waveform, local glow, and reduced-motion behavior. Hidden recording controls must collapse out of processing/success layout.
2. Extend `tests/test_status_agent_config.py` with one focused source-contract test for the approved layout and accessibility behavior.

## Verification

- Focused status-agent tests pass.
- The native status agent builds successfully.
- `make dev-install` installs the change into the local runtime.
- Installed-runtime smoke confirms the status agent is running and the overlay states render without layout regression.

## Constraints

- No new dependency or parallel UI implementation.
- Preserve existing uncommitted dictation work; do not stash, reset, or commit it.
- Keep longer error/status copy readable with bounded fallback sizing.
