# Distribute outside the App Store without Sandbox

The first self-contained Yulu release uses Developer ID distribution and does not
enable App Sandbox or target the Mac App Store. This preserves explicit access to
user-selected recording folders, local Agent runtimes, MCP, and migrated data
while the new Application Runtime and permission boundaries are stabilized.
