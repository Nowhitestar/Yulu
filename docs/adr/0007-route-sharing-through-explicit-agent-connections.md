# Route sharing through explicit Agent Connections

New Notion and Zulip Share Actions use an explicitly selected Agent Connection
whose connector capability has been proven, rather than depending on Hermes or
the legacy general Agent runtime. Each external write still requires user
confirmation and a verifiable receipt; the existing Hermes-only automatic Notion
path remains a compatibility path and is not part of guided onboarding.
