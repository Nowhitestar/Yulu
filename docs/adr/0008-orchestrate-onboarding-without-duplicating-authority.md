# Orchestrate onboarding without duplicating authority

Yulu adds one resumable Onboarding Home that links into Core Activation, Agent
Connection Center, calendar, and sharing surfaces while leaving each surface
authoritative for its own state. Fresh installations open this journey after a
healthy startup; upgrades and returning users receive only non-blocking entry,
so guidance improves without creating parallel configuration or forced re-entry.
