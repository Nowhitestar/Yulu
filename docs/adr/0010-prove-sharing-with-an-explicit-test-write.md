# Prove sharing with an explicit test write

Sharing becomes adopted for Onboarding only after the user explicitly sends a
Test Share containing no meeting content and Yulu verifies its receipt. Target
discovery alone cannot prove write permission, while an automatic probe would
create an external side effect without consent; users who do not want the test
can defer sharing instead.
